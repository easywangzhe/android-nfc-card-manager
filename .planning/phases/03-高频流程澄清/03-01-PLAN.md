---
phase: 03-高频流程澄清
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/java/com/opencode/nfccardmanager/core/nfc/model/HighFrequencyFlowGuidance.kt
  - app/src/test/java/com/opencode/nfccardmanager/core/nfc/HighFrequencyFlowGuidanceTest.kt
autonomous: true
requirements:
  - FLOW-05
  - RISK-04
must_haves:
  truths:
    - 写卡、读卡结果、格式化页可以从同一套规则得到推荐下一步，而不是各写一套文案
    - 格式化失败状态码可被稳定映射为“发生了什么 / 为什么 / 当前最安全下一步”
    - 写卡结果能稳定区分写入执行结果与回读校验结果的推荐动作
  artifacts:
    - path: app/src/main/java/com/opencode/nfccardmanager/core/nfc/model/HighFrequencyFlowGuidance.kt
      provides: 高频流程结果与下一步指导共享契约
      exports:
        - WriteOutcomeGuidance
        - FlowNextStepGuidance
        - buildWriteOutcomeGuidance
        - buildReadNextStepGuidance
        - buildFormatNextStepGuidance
    - path: app/src/test/java/com/opencode/nfccardmanager/core/nfc/HighFrequencyFlowGuidanceTest.kt
      provides: 高频流程指导契约回归测试
      contains: 写卡执行/校验拆分、读卡状态推荐、格式化失败安全动作断言
  key_links:
    - from: app/src/main/java/com/opencode/nfccardmanager/core/nfc/model/HighFrequencyFlowGuidance.kt
      to: app/src/main/java/com/opencode/nfccardmanager/core/nfc/model/WriteModels.kt
      via: WriteCardResult.writeStatus/verified
      pattern: writeStatus|verified|verificationMessage
    - from: app/src/main/java/com/opencode/nfccardmanager/core/nfc/model/HighFrequencyFlowGuidance.kt
      to: app/src/main/java/com/opencode/nfccardmanager/core/nfc/model/FormatModels.kt
      via: FormatCardResult.status/reason
      pattern: status|reason
    - from: app/src/test/java/com/opencode/nfccardmanager/core/nfc/HighFrequencyFlowGuidanceTest.kt
      to: app/src/main/java/com/opencode/nfccardmanager/core/nfc/model/HighFrequencyFlowGuidance.kt
      via: 纯 Kotlin 指导映射
      pattern: build(WriteOutcomeGuidance|ReadNextStepGuidance|FormatNextStepGuidance)
---

<objective>
先把 Phase 3 需要的“结果差异 + 推荐下一步”抽成纯 Kotlin 共享契约，给写卡、读卡结果、格式化三个页面提供同一套 truth source。

Purpose: 避免三个页面分别硬编码状态判断，确保 `FLOW-05` 与 `RISK-04` 的推荐动作在不同页面中语义一致。
Output: 一个共享指导契约文件，以及覆盖写卡/读卡/格式化典型状态的单元测试。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-高频流程澄清/03-DISCOVERY.md
@.planning/phases/01-基础治理与边界收口/01-03-SUMMARY.md
@app/src/main/java/com/opencode/nfccardmanager/core/nfc/model/WriteModels.kt
@app/src/main/java/com/opencode/nfccardmanager/core/nfc/model/ReadModels.kt
@app/src/main/java/com/opencode/nfccardmanager/core/nfc/model/FormatModels.kt

<interfaces>
From `app/src/main/java/com/opencode/nfccardmanager/core/nfc/model/WriteModels.kt`:
```kotlin
data class WriteCardResult(
    val success: Boolean,
    val message: String,
    val writeStatus: String = "UNKNOWN",
    val writeReason: String = "",
    val verified: Boolean = false,
    val verificationMessage: String = "未校验",
)
```

From `app/src/main/java/com/opencode/nfccardmanager/core/nfc/model/ReadModels.kt`:
```kotlin
data class ReadCardResult(
    val capability: CardCapability,
    val readStatus: String = "UNKNOWN",
    val readReason: String = "",
)
```

From `app/src/main/java/com/opencode/nfccardmanager/core/nfc/model/FormatModels.kt`:
```kotlin
data class FormatCardResult(
    val success: Boolean,
    val message: String,
    val status: String,
    val reason: String,
)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 先写高频流程指导契约测试骨架</name>
  <files>app/src/main/java/com/opencode/nfccardmanager/core/nfc/model/HighFrequencyFlowGuidance.kt, app/src/test/java/com/opencode/nfccardmanager/core/nfc/HighFrequencyFlowGuidanceTest.kt</files>
  <behavior>
    - Test 1: `WRITE_SUCCESS` 映射为“写入执行成功 + 回读校验通过”，并给出继续写卡/返回业务流的推荐动作。
    - Test 2: `VERIFY_FAILED` 映射为“写入已执行但回读校验失败”，不能被当成普通写入异常处理。
    - Test 3: `READ_ERROR`、`EMPTY_NDEF`、`NON_NDEF` 分别返回不同的下一步指导文案。
    - Test 4: `UNSUPPORTED_TAG`、`CLEAR_ERROR`、`FORMAT_ERROR` 返回不同的格式化失败原因标签与最安全动作。
  </behavior>
  <action>先创建 `HighFrequencyFlowGuidance.kt` 的最小导出骨架，再写 `HighFrequencyFlowGuidanceTest.kt`。测试必须覆盖写卡结果拆分、读卡推荐动作和格式化失败安全动作三组场景，且显式断言推荐动作文案/标识，而不是只断言布尔值，避免后续页面重新拼接文案。</action>
  <verify>
    <automated>./gradlew app:testDebugUnitTest --tests "*HighFrequencyFlowGuidanceTest"</automated>
  </verify>
  <done>测试文件已描述 Phase 3 所需的关键指导语义，且在实现前处于失败或待实现状态。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现共享的结果拆分与下一步指导契约</name>
  <files>app/src/main/java/com/opencode/nfccardmanager/core/nfc/model/HighFrequencyFlowGuidance.kt, app/src/test/java/com/opencode/nfccardmanager/core/nfc/HighFrequencyFlowGuidanceTest.kt</files>
  <behavior>
    - Test 1: `buildWriteOutcomeGuidance` 对 `WRITE_SUCCESS` / `VERIFY_FAILED` / `WRITE_ERROR` 给出不同的执行结果、校验结果和下一步动作。
    - Test 2: `buildReadNextStepGuidance` 对 `READ_SUCCESS` / `EMPTY_NDEF` / `NON_NDEF` / `READ_ERROR` 给出稳定的推荐动作。
    - Test 3: `buildFormatNextStepGuidance` 对 `CLEARED_NDEF` / `FORMAT_SUCCESS` / `UNSUPPORTED_TAG` / `CLEAR_ERROR` / `FORMAT_ERROR` 给出可直接展示的用户结论与安全动作。
  </behavior>
  <action>在 `HighFrequencyFlowGuidance.kt` 中实现纯 Kotlin 数据模型和映射函数，统一输出可直接给 UI 渲染的标题、结论、原因摘要、推荐下一步、CTA 语义。必须直接消费 `WriteCardResult.writeStatus/verified`、`ReadCardResult.readStatus`、`FormatCardResult.status` 这些既有 truth source；不要在页面层复制 status-code 判定，也不要改写 `NdefWriter` / `NdefFormatter` 的底层状态码。</action>
  <verify>
    <automated>./gradlew app:testDebugUnitTest --tests "*HighFrequencyFlowGuidanceTest"</automated>
  </verify>
  <done>共享契约已能稳定输出写卡、读卡、格式化三类高频流程的推荐动作与差异说明，测试全部通过。</done>
</task>

</tasks>

<verification>
- `HighFrequencyFlowGuidanceTest` 通过，覆盖写卡执行/校验拆分、读卡推荐动作、格式化失败安全动作。
- 契约实现保持纯 Kotlin，无 Compose 依赖，可被多个页面直接复用。
</verification>

<success_criteria>
- Phase 3 后续页面改造有单一“下一步指导”来源。
- `FLOW-05` 与 `RISK-04` 的核心决策语义不再散落在三个页面里各自维护。
</success_criteria>

<output>
After completion, create `.planning/phases/03-高频流程澄清/03-01-SUMMARY.md`
</output>
