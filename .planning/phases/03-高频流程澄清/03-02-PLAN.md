---
phase: 03-高频流程澄清
plan: "02"
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - app/src/test/java/com/opencode/nfccardmanager/feature/write/WriteViewModelPhase3Test.kt
  - app/src/main/java/com/opencode/nfccardmanager/feature/write/WriteContract.kt
  - app/src/main/java/com/opencode/nfccardmanager/feature/write/WriteViewModel.kt
  - app/src/main/java/com/opencode/nfccardmanager/feature/write/WriteEditorScreen.kt
autonomous: true
requirements:
  - FLOW-04
  - FLOW-05
must_haves:
  truths:
    - 用户完成写卡后，能同时看见“写入执行结果”和“回读校验结果”，不会把两者混为一个成功/失败结论
    - 写卡失败、校验失败、需先格式化等状态都有明确下一步建议
    - 写卡页保留 Phase 1 的真实性提示，同时不掩盖低层 `writeStatus` 与 `verificationMessage` 的真实含义
  artifacts:
    - path: app/src/main/java/com/opencode/nfccardmanager/feature/write/WriteContract.kt
      provides: 写卡页结果拆分与指导状态字段
      contains: 写入执行结果、回读校验结果、推荐下一步所需状态
    - path: app/src/main/java/com/opencode/nfccardmanager/feature/write/WriteViewModel.kt
      provides: 写卡结果到 UI 指导模型的映射
      contains: 对 HighFrequencyFlowGuidance 的消费
    - path: app/src/main/java/com/opencode/nfccardmanager/feature/write/WriteEditorScreen.kt
      provides: 写卡结果拆分 UI 与推荐动作区
      contains: 执行结果区、校验结果区、下一步建议区
    - path: app/src/test/java/com/opencode/nfccardmanager/feature/write/WriteViewModelPhase3Test.kt
      provides: 写卡结果拆分回归测试
      contains: WRITE_SUCCESS / VERIFY_FAILED / WRITE_ERROR 状态断言
  key_links:
    - from: app/src/main/java/com/opencode/nfccardmanager/feature/write/WriteViewModel.kt
      to: app/src/main/java/com/opencode/nfccardmanager/core/nfc/model/HighFrequencyFlowGuidance.kt
      via: buildWriteOutcomeGuidance
      pattern: buildWriteOutcomeGuidance
    - from: app/src/main/java/com/opencode/nfccardmanager/feature/write/WriteEditorScreen.kt
      to: app/src/main/java/com/opencode/nfccardmanager/feature/write/WriteViewModel.kt
      via: uiState 新结果字段
      pattern: execution|verification|nextStep|guidance
---

<objective>
把写卡页结果展示升级为“写入执行结果 + 回读校验结果 + 推荐下一步”的三段表达，直接满足 `FLOW-04` 与写卡场景的 `FLOW-05`。

Purpose: 写卡是高频主路径，必须先让用户明确知道命令是否执行、校验是否通过，以及当前最安全的后续动作。
Output: 写卡页新增状态字段、ViewModel 映射和结果区 UI，附带针对关键状态的单元测试。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-高频流程澄清/03-DISCOVERY.md
@.planning/phases/03-高频流程澄清/03-01-PLAN.md
@app/src/main/java/com/opencode/nfccardmanager/feature/write/WriteContract.kt
@app/src/main/java/com/opencode/nfccardmanager/feature/write/WriteViewModel.kt
@app/src/main/java/com/opencode/nfccardmanager/feature/write/WriteEditorScreen.kt

<interfaces>
From `app/src/main/java/com/opencode/nfccardmanager/feature/write/WriteContract.kt`:
```kotlin
enum class WriteStage {
    IDLE,
    READY,
    WRITING,
    SUCCESS,
    ERROR,
}

data class WriteUiState(
    val content: String = "",
    val stage: WriteStage = WriteStage.IDLE,
    val message: String = "请输入待写入的文本内容，确认后点击开始写卡。",
    val result: WriteCardResult? = null,
)
```

From `app/src/main/java/com/opencode/nfccardmanager/core/nfc/model/WriteModels.kt`:
```kotlin
data class WriteCardResult(
    val success: Boolean,
    val message: String,
    val writeStatus: String = "UNKNOWN",
    val writeReason: String = "",
    val verified: Boolean = false,
    val verificationMessage: String = "未校验",
)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 先锁定写卡结果拆分的 ViewModel 回归测试</name>
  <files>app/src/test/java/com/opencode/nfccardmanager/feature/write/WriteViewModelPhase3Test.kt, app/src/main/java/com/opencode/nfccardmanager/feature/write/WriteContract.kt, app/src/main/java/com/opencode/nfccardmanager/feature/write/WriteViewModel.kt</files>
  <behavior>
    - Test 1: `onWriteResult(WRITE_SUCCESS)` 后，UI 同时暴露“写入执行成功”“回读校验通过”和继续操作建议。
    - Test 2: `onWriteResult(VERIFY_FAILED)` 后，UI 明确显示“写入已执行，但回读校验失败”，且推荐先重新贴卡复核/重试，而不是笼统写成“写卡失败”。
    - Test 3: `onError(...)` 或 `WRITE_ERROR` 后，UI 推荐先排查标签稳定性或卡片兼容性。
  </behavior>
  <action>新增 `WriteViewModelPhase3Test.kt`，优先断言 `WriteViewModel` 输出的结果拆分字段与推荐动作。测试不要直接依赖 Compose；只围绕 `WriteUiState` 做断言，这样后续 UI 改造可以安全消费这些状态。</action>
  <verify>
    <automated>./gradlew app:testDebugUnitTest --tests "*WriteViewModelPhase3Test"</automated>
  </verify>
  <done>测试已覆盖写卡执行成功、校验失败、写入异常三类关键状态，且在实现前失败或待实现。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 在写卡状态层接入共享指导契约</name>
  <files>app/src/test/java/com/opencode/nfccardmanager/feature/write/WriteViewModelPhase3Test.kt, app/src/main/java/com/opencode/nfccardmanager/feature/write/WriteContract.kt, app/src/main/java/com/opencode/nfccardmanager/feature/write/WriteViewModel.kt</files>
  <behavior>
    - Test 1: `WriteUiState` 能承载执行结果、校验结果、推荐下一步和推荐动作标题。
    - Test 2: `WriteViewModel.onWriteResult()` 直接消费 `buildWriteOutcomeGuidance`，不在 ViewModel 里重新散写 `when(writeStatus)` 文案。
  </behavior>
  <action>扩展 `WriteContract.kt`，新增可供 UI 直接渲染的结果拆分字段（如执行结果摘要、校验结果摘要、推荐下一步）。在 `WriteViewModel.kt` 中把 `WriteCardResult` 映射到 Plan 01 的共享指导契约，确保 `VERIFY_FAILED` 仍保留失败态语义，但文案层明确写出“写入命令已执行、仅校验未通过”。不要改写 `NdefWriter` 的状态码，也不要把下一步建议硬编码在 `WriteEditorScreen.kt`。</action>
  <verify>
    <automated>./gradlew app:testDebugUnitTest --tests "*WriteViewModelPhase3Test"</automated>
  </verify>
  <done>写卡 ViewModel 已能稳定输出执行结果/校验结果/推荐动作三组信息，测试通过。</done>
</task>

<task type="auto">
  <name>Task 3: 重构写卡结果区为三段式结果与推荐动作</name>
  <files>app/src/main/java/com/opencode/nfccardmanager/feature/write/WriteEditorScreen.kt</files>
  <action>更新 `WriteEditorScreen.kt` 的结果区与“下一步操作”区：保留 Phase 1 的共享阶段和真实性胶囊，同时新增“写入执行结果”“回读校验结果”“推荐下一步”三段表达。对 `WRITE_SUCCESS` 要同时展示执行成功与校验通过；对 `VERIFY_FAILED` 要显示写入已执行但当前不建议直接离场；对 `UNSUPPORTED_TAG`/`FORMAT_ERROR` 要提示先更换卡片或先格式化。避免仅凭 `result.success` 决定整块 UI 文案，也不要删除原有 `writeReason` / `verificationMessage` 细节。</action>
  <verify>
    <automated>./gradlew assembleDebug</automated>
  </verify>
  <done>写卡页结果区已能一眼区分写入执行结果与回读校验结果，并展示对应下一步建议。</done>
</task>

</tasks>

<verification>
- `WriteViewModelPhase3Test` 通过，覆盖写卡成功、校验失败、写入异常三类关键状态。
- `assembleDebug` 通过，说明写卡页的新状态字段与 UI wiring 完整可编译。
</verification>

<success_criteria>
- 用户在写卡页可明确区分“写入成功”与“回读校验成功”两个结果。
- 用户在写卡异常、校验失败或需格式化时都能看到明确下一步建议。
</success_criteria>

<output>
After completion, create `.planning/phases/03-高频流程澄清/03-02-SUMMARY.md`
</output>
