---
phase: 03-高频流程澄清
plan: "03"
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - app/src/test/java/com/opencode/nfccardmanager/feature/format/FormatViewModelPhase3Test.kt
  - app/src/main/java/com/opencode/nfccardmanager/feature/format/FormatContract.kt
  - app/src/main/java/com/opencode/nfccardmanager/feature/format/FormatViewModel.kt
  - app/src/main/java/com/opencode/nfccardmanager/feature/format/FormatCardScreen.kt
  - app/src/main/java/com/opencode/nfccardmanager/feature/read/ReadResultScreen.kt
autonomous: true
requirements:
  - FLOW-05
  - RISK-04
must_haves:
  truths:
    - 用户在读卡结果页能看到与 `readStatus` 匹配的下一步建议，而不是固定的模糊按钮组合
    - 用户在格式化失败时能看懂失败发生了什么、可能原因和当前最安全动作
    - 用户在格式化成功后能明确知道可以继续去写卡，且失败时不会误以为卡片已安全清空
  artifacts:
    - path: app/src/main/java/com/opencode/nfccardmanager/feature/format/FormatContract.kt
      provides: 格式化结果指导状态字段
      contains: 失败原因分类与安全下一步字段
    - path: app/src/main/java/com/opencode/nfccardmanager/feature/format/FormatViewModel.kt
      provides: 格式化结果到指导模型的映射
      contains: 对 buildFormatNextStepGuidance 的消费
    - path: app/src/main/java/com/opencode/nfccardmanager/feature/format/FormatCardScreen.kt
      provides: 格式化结果诊断与安全动作区
      contains: 失败结论、原因、最安全下一步 UI
    - path: app/src/main/java/com/opencode/nfccardmanager/feature/read/ReadResultScreen.kt
      provides: 读卡结果推荐下一步区
      contains: 针对 READ_SUCCESS/EMPTY_NDEF/NON_NDEF/READ_ERROR 的推荐动作
    - path: app/src/test/java/com/opencode/nfccardmanager/feature/format/FormatViewModelPhase3Test.kt
      provides: 格式化结果指导回归测试
      contains: FORMAT_SUCCESS/UNSUPPORTED_TAG/CLEAR_ERROR/FORMAT_ERROR 断言
  key_links:
    - from: app/src/main/java/com/opencode/nfccardmanager/feature/format/FormatViewModel.kt
      to: app/src/main/java/com/opencode/nfccardmanager/core/nfc/model/HighFrequencyFlowGuidance.kt
      via: buildFormatNextStepGuidance
      pattern: buildFormatNextStepGuidance
    - from: app/src/main/java/com/opencode/nfccardmanager/feature/read/ReadResultScreen.kt
      to: app/src/main/java/com/opencode/nfccardmanager/core/nfc/model/HighFrequencyFlowGuidance.kt
      via: buildReadNextStepGuidance
      pattern: buildReadNextStepGuidance
---

<objective>
为读卡结果页和格式化页补齐状态差异、失败原因与安全后续动作，让用户在高频流程收尾时能明确知道接下来该做什么。

Purpose: `FLOW-05` 与 `RISK-04` 的价值主要体现在结果页，如果读卡/格式化结果仍然模糊，用户会继续靠猜测操作。
Output: 格式化页结果指导状态、读卡结果推荐动作区，以及对应的 ViewModel 单元测试。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-高频流程澄清/03-DISCOVERY.md
@.planning/phases/03-高频流程澄清/03-01-PLAN.md
@app/src/main/java/com/opencode/nfccardmanager/feature/format/FormatContract.kt
@app/src/main/java/com/opencode/nfccardmanager/feature/format/FormatViewModel.kt
@app/src/main/java/com/opencode/nfccardmanager/feature/format/FormatCardScreen.kt
@app/src/main/java/com/opencode/nfccardmanager/feature/read/ReadResultScreen.kt

<interfaces>
From `app/src/main/java/com/opencode/nfccardmanager/feature/format/FormatContract.kt`:
```kotlin
data class FormatUiState(
    val stage: FormatStage = FormatStage.IDLE,
    val message: String = "点击开始格式化后，再将待处理卡片贴近手机背部。",
    val result: FormatCardResult? = null,
)
```

From `app/src/main/java/com/opencode/nfccardmanager/core/nfc/model/FormatModels.kt`:
```kotlin
data class FormatCardResult(
    val success: Boolean,
    val message: String,
    val status: String,
    val reason: String,
)
```

From `app/src/main/java/com/opencode/nfccardmanager/core/nfc/model/ReadModels.kt`:
```kotlin
data class ReadCardResult(
    val capability: CardCapability,
    val readStatus: String = "UNKNOWN",
    val readReason: String = "",
)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 先锁定格式化结果指导的 ViewModel 测试</name>
  <files>app/src/test/java/com/opencode/nfccardmanager/feature/format/FormatViewModelPhase3Test.kt, app/src/main/java/com/opencode/nfccardmanager/feature/format/FormatContract.kt, app/src/main/java/com/opencode/nfccardmanager/feature/format/FormatViewModel.kt</files>
  <behavior>
    - Test 1: `FORMAT_SUCCESS` / `CLEARED_NDEF` 后，UI 给出“可以继续去写卡”的推荐动作。
    - Test 2: `UNSUPPORTED_TAG` 后，UI 明确提示停止继续格式化/写卡并更换支持 NDEF 的卡片。
    - Test 3: `CLEAR_ERROR` / `FORMAT_ERROR` 后，UI 明确提示失败已发生、需要稳定贴卡后重试，而不是假设已清空成功。
  </behavior>
  <action>新增 `FormatViewModelPhase3Test.kt`，先把格式化成功、卡片不支持、清空异常、格式化异常四类关键状态的期望指导锁定。测试只断言 `FormatUiState` 暴露的结果指导字段，不引入 Compose 层依赖。</action>
  <verify>
    <automated>./gradlew app:testDebugUnitTest --tests "*FormatViewModelPhase3Test"</automated>
  </verify>
  <done>测试已覆盖格式化页关键状态的推荐动作与失败表达，且在实现前失败或待实现。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 在格式化状态层与页面接入失败诊断和安全动作</name>
  <files>app/src/test/java/com/opencode/nfccardmanager/feature/format/FormatViewModelPhase3Test.kt, app/src/main/java/com/opencode/nfccardmanager/feature/format/FormatContract.kt, app/src/main/java/com/opencode/nfccardmanager/feature/format/FormatViewModel.kt, app/src/main/java/com/opencode/nfccardmanager/feature/format/FormatCardScreen.kt</files>
  <behavior>
    - Test 1: `FormatViewModel.onFormatResult()` 直接消费 `buildFormatNextStepGuidance` 输出失败结论与安全动作。
    - Test 2: `FormatCardScreen` 结果区渲染“发生了什么 / 为什么 / 当前最安全下一步”三段内容，并保留成功后“去写卡”动作。
  </behavior>
  <action>扩展 `FormatContract.kt` 和 `FormatViewModel.kt`，新增用于展示失败结论、原因标签、推荐下一步的状态字段，并直接消费 Plan 01 的共享指导契约。然后更新 `FormatCardScreen.kt`：成功态继续保留“去写卡”按钮；失败态新增显式诊断区，针对 `UNSUPPORTED_TAG`、`CLEAR_ERROR`、`FORMAT_ERROR` 显示不同的安全建议。不要仅原样重复 `reason`，也不要在失败时保留容易让人误以为卡片已清空的成功式文案。</action>
  <verify>
    <automated>./gradlew app:testDebugUnitTest --tests "*FormatViewModelPhase3Test"</automated>
  </verify>
  <done>格式化页已能清楚表达失败是否发生、原因分类和最安全后续动作，测试通过。</done>
</task>

<task type="auto">
  <name>Task 3: 让读卡结果页按读卡状态给出明确下一步建议</name>
  <files>app/src/main/java/com/opencode/nfccardmanager/feature/read/ReadResultScreen.kt</files>
  <action>更新 `ReadResultScreen.kt` 的“下一步操作”与摘要说明区，直接消费共享指导契约，根据 `READ_SUCCESS`、`EMPTY_NDEF`、`NON_NDEF`、`READ_ERROR` 渲染不同的推荐动作说明。保留当前可用按钮边界（不强制新增新路由），但要让按钮文案排序和说明文案与推荐动作一致，避免所有结果页都只显示同样的“格式化卡 / 重新读卡”组合。</action>
  <verify>
    <automated>./gradlew assembleDebug</automated>
  </verify>
  <done>读卡结果页已能根据真实 `readStatus` 给出明确后续建议，格式化页失败时也能给出安全动作提示。</done>
</task>

</tasks>

<verification>
- `FormatViewModelPhase3Test` 通过，覆盖格式化成功、卡片不支持、清空异常、格式化异常。
- `assembleDebug` 通过，说明读卡结果页与格式化页的新指导状态 wiring 完整可编译。
</verification>

<success_criteria>
- 用户在读卡结果页、格式化页都能看到与当前状态匹配的推荐下一步。
- 用户在格式化失败时能明确知道失败发生了什么、可能原因和当前最安全动作。
</success_criteria>

<output>
After completion, create `.planning/phases/03-高频流程澄清/03-03-SUMMARY.md`
</output>
