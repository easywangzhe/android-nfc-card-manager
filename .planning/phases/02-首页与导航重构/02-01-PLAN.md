---
phase: 02-首页与导航重构
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/java/com/opencode/nfccardmanager/feature/home/HomeShellContract.kt
  - app/src/test/java/com/opencode/nfccardmanager/feature/home/HomeShellContractTest.kt
autonomous: true
requirements:
  - SHELL-02
must_haves:
  truths:
    - 当前角色只能拿到自己可见的首页入口与底部导航项定义
    - 主任务、高风险操作、管理工具在共享模型中有稳定分组，不再靠页面硬编码顺序猜测
    - 审计员不会再被建模为可执行读卡/写卡/高风险入口
  artifacts:
    - path: app/src/main/java/com/opencode/nfccardmanager/feature/home/HomeShellContract.kt
      provides: 角色感知的首页分组与导航项契约
      exports:
        - HomeSection
        - HomeEntry
        - BottomNavDestination
        - buildHomeSections
        - buildBottomNavDestinations
    - path: app/src/test/java/com/opencode/nfccardmanager/feature/home/HomeShellContractTest.kt
      provides: 角色可见性与分组回归测试
      contains: OPERATOR、SUPERVISOR、ADMIN、AUDITOR 可见性断言
  key_links:
    - from: app/src/main/java/com/opencode/nfccardmanager/feature/home/HomeShellContract.kt
      to: app/src/main/java/com/opencode/nfccardmanager/core/security/SecurityManager.kt
      via: canRead/canWrite/canLock/canUnlock/canManageTemplate/canViewAudit
      pattern: SecurityManager\.(canRead|canWrite|canLock|canUnlock|canManageTemplate|canViewAudit)
    - from: app/src/test/java/com/opencode/nfccardmanager/feature/home/HomeShellContractTest.kt
      to: app/src/main/java/com/opencode/nfccardmanager/feature/home/HomeShellContract.kt
      via: buildHomeSections/buildBottomNavDestinations
      pattern: build(HomeSections|BottomNavDestinations)
---

<objective>
为首页与底部导航先定义统一的“角色可见入口契约”，让后续 UI 重构直接消费同一组分区与权限规则。

Purpose: 先把分组、风险层级和角色可见性收口到共享模型，避免 HomeScreen 与 AppNavGraph 各自维护一套入口判断。
Output: 可被首页与导航同时复用的入口契约文件，以及覆盖角色可见性的单元测试。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-首页与导航重构/02-DISCOVERY.md
@.planning/phases/01-基础治理与边界收口/01-03-SUMMARY.md
@app/src/main/java/com/opencode/nfccardmanager/core/security/SecurityManager.kt

<interfaces>
From `app/src/main/java/com/opencode/nfccardmanager/core/security/SecurityManager.kt`:
```kotlin
enum class UserRole {
    OPERATOR,
    SUPERVISOR,
    ADMIN,
    AUDITOR,
}

fun canRead(role: UserRole): Boolean
fun canWrite(role: UserRole): Boolean
fun canLock(role: UserRole): Boolean
fun canUnlock(role: UserRole): Boolean
fun canManageTemplate(role: UserRole): Boolean
fun canViewAudit(role: UserRole): Boolean
fun roleLabel(role: UserRole): String
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 写首页/导航入口契约测试骨架</name>
  <files>app/src/main/java/com/opencode/nfccardmanager/feature/home/HomeShellContract.kt, app/src/test/java/com/opencode/nfccardmanager/feature/home/HomeShellContractTest.kt</files>
  <behavior>
    - Test 1: OPERATOR 只得到读卡、写卡、日志、设置入口，不得到锁卡、解锁、模板。
    - Test 2: SUPERVISOR 得到读卡、写卡、锁卡、解锁、日志、设置，但不得到模板。
    - Test 3: ADMIN 得到全部首页分组与模板导航项。
    - Test 4: AUDITOR 只得到管理工具分组中的日志、设置，且底部导航不出现模板。
  </behavior>
  <action>先创建 `HomeShellContract.kt` 的最小导出骨架（`HomeSection`、`HomeEntry`、`BottomNavDestination` 及 `buildHomeSections` / `buildBottomNavDestinations` 签名），再写 `HomeShellContractTest.kt`，用现有 `UserRole` 与 `SecurityManager` 权限规则驱动断言。测试中必须显式断言“主任务 / 高风险 / 管理工具”三类分组的出现与缺失，避免后续 UI 仍用扁平快捷入口。</action>
  <verify>
    <automated>./gradlew app:testDebugUnitTest --tests "*HomeShellContractTest"</automated>
  </verify>
  <done>测试文件已描述四类角色的入口可见性与分组预期，且在实现前处于失败或待实现状态。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现角色感知的首页分组与底部导航契约</name>
  <files>app/src/main/java/com/opencode/nfccardmanager/feature/home/HomeShellContract.kt, app/src/test/java/com/opencode/nfccardmanager/feature/home/HomeShellContractTest.kt</files>
  <behavior>
    - Test 1: `buildHomeSections(UserRole.OPERATOR)` 返回主任务 + 管理工具，不返回高风险分组。
    - Test 2: `buildHomeSections(UserRole.AUDITOR)` 仅返回日志、设置所在的管理工具分组。
    - Test 3: `buildBottomNavDestinations(UserRole.ADMIN)` 包含模板导航项，`UserRole.SUPERVISOR` 与 `UserRole.OPERATOR` 不包含。
  </behavior>
  <action>实现共享契约文件：定义首页分组枚举/数据类、入口说明文案、风险语义字段和导航目标标识；内部统一调用 `SecurityManager` 的现有权限函数，而不是复制 `when(role)` 规则。入口定义需覆盖首页三类分组和底部导航四个一级页，并确保“模板仅管理员可见、日志/设置全角色可见、审计员无主任务/高风险入口”。保持该文件纯 Kotlin，可被 `HomeScreen` 与 `AppNavGraph` 直接消费。</action>
  <verify>
    <automated>./gradlew app:testDebugUnitTest --tests "*HomeShellContractTest"</automated>
  </verify>
  <done>共享契约可稳定输出角色可见入口与分组，测试全部通过，且没有在页面层重复写角色分支。</done>
</task>

</tasks>

<verification>
- `HomeShellContractTest` 通过，覆盖四类角色的首页入口与底部导航可见性。
- 契约实现只依赖现有 `SecurityManager`，不引入新的权限源。
</verification>

<success_criteria>
- Phase 2 的首页分区与导航裁剪有单一契约来源。
- SHELL-02 所需的“无权限首页入口不显示”为后续 UI 落地准备好可执行接口。
</success_criteria>

<output>
After completion, create `.planning/phases/02-首页与导航重构/02-01-SUMMARY.md`
</output>
