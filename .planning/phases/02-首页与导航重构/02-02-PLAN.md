---
phase: 02-首页与导航重构
plan: "02"
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - app/src/main/java/com/opencode/nfccardmanager/feature/home/HomeScreen.kt
  - app/src/main/java/com/opencode/nfccardmanager/navigation/AppNavGraph.kt
  - app/src/main/java/com/opencode/nfccardmanager/ui/component/AppUi.kt
autonomous: true
requirements:
  - SHELL-01
  - SHELL-02
must_haves:
  truths:
    - 用户打开首页后，能直接看出主任务、高风险操作、管理工具是三个不同区域
    - 当前角色无权使用的首页入口不会再显示成可点击主入口
    - 底部导航只保留当前角色可访问的一级页，避免进入后才被拒绝
  artifacts:
    - path: app/src/main/java/com/opencode/nfccardmanager/feature/home/HomeScreen.kt
      provides: 按分组展示首页入口与说明的首页界面
      contains: 主任务 / 高风险操作 / 管理工具 分区渲染
    - path: app/src/main/java/com/opencode/nfccardmanager/navigation/AppNavGraph.kt
      provides: 角色感知的底部导航与首页 wiring
      contains: buildBottomNavDestinations
    - path: app/src/main/java/com/opencode/nfccardmanager/ui/component/AppUi.kt
      provides: 首页分组卡片和风险语义可复用组件
      contains: 分组标题或入口卡片扩展组件
  key_links:
    - from: app/src/main/java/com/opencode/nfccardmanager/feature/home/HomeScreen.kt
      to: app/src/main/java/com/opencode/nfccardmanager/feature/home/HomeShellContract.kt
      via: buildHomeSections(currentRole)
      pattern: buildHomeSections\(currentRole\)
    - from: app/src/main/java/com/opencode/nfccardmanager/navigation/AppNavGraph.kt
      to: app/src/main/java/com/opencode/nfccardmanager/feature/home/HomeShellContract.kt
      via: buildBottomNavDestinations(currentRole)
      pattern: buildBottomNavDestinations\(currentRole\)
    - from: app/src/main/java/com/opencode/nfccardmanager/navigation/AppNavGraph.kt
      to: app/src/main/java/com/opencode/nfccardmanager/feature/home/HomeScreen.kt
      via: onTemplate/onAudit/onSettings callbacks
      pattern: HomeScreen\(
---

<objective>
把首页与底部导航改造成角色感知、分区清晰的入口壳层，让用户一进应用就能区分主任务、高风险和管理工具。

Purpose: 直接落地 SHELL-01 / SHELL-02，让首页与导航结构成为后续流程页优化的稳定壳层，而不是继续充当扁平工具面板。
Output: 重构后的首页分区 UI、复用组件，以及按角色裁剪的底部导航 wiring。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-首页与导航重构/02-DISCOVERY.md
@.planning/phases/01-基础治理与边界收口/01-03-SUMMARY.md
@app/src/main/java/com/opencode/nfccardmanager/feature/home/HomeScreen.kt
@app/src/main/java/com/opencode/nfccardmanager/navigation/AppNavGraph.kt
@app/src/main/java/com/opencode/nfccardmanager/ui/component/AppUi.kt

<interfaces>
From `app/src/main/java/com/opencode/nfccardmanager/feature/home/HomeScreen.kt`:
```kotlin
fun HomeScreen(
    currentSession: UserSession,
    currentRole: UserRole,
    roleLabel: (UserRole) -> String,
    onRoleChange: (UserRole) -> Unit,
    onLogout: () -> Unit,
    onReadClick: () -> Unit,
    onWriteClick: () -> Unit,
    onLockClick: () -> Unit,
    onUnlockClick: () -> Unit,
    canRead: Boolean,
    canWrite: Boolean,
    canLock: Boolean,
    canUnlock: Boolean,
)
```

From `app/src/main/java/com/opencode/nfccardmanager/navigation/AppNavGraph.kt`:
```kotlin
object Routes {
    const val HOME = "home"
    const val TEMPLATE = "template"
    const val SETTINGS = "settings"
    const val AUDIT = "audit"
    fun scan(mode: ScanMode): String
}
```

Expected from Plan 01 (`HomeShellContract.kt`):
```kotlin
fun buildHomeSections(role: UserRole): List<HomeSection>
fun buildBottomNavDestinations(role: UserRole): List<BottomNavDestination>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 扩展首页分组与入口卡片组件</name>
  <files>app/src/main/java/com/opencode/nfccardmanager/ui/component/AppUi.kt</files>
  <action>在 `AppUi.kt` 中新增或扩展可复用组件，支撑首页的“分组标题 + 分组说明 + 入口卡片 + 风险语义”表达。高风险入口必须使用明显的 warning / danger 视觉语义，避免继续复用和主任务完全一致的按钮样式；同时保持组件足够轻量，只服务首页壳层，不引入新的设计系统或复杂主题层。</action>
  <verify>
    <automated>./gradlew assembleDebug</automated>
  </verify>
  <done>`AppUi.kt` 已提供可被首页复用的分组/入口表现组件，且高风险入口样式与主任务有可见区分。</done>
</task>

<task type="auto">
  <name>Task 2: 首页改为角色感知的三段式信息层级</name>
  <files>app/src/main/java/com/opencode/nfccardmanager/feature/home/HomeScreen.kt, app/src/main/java/com/opencode/nfccardmanager/ui/component/AppUi.kt</files>
  <action>重构 `HomeScreen.kt` 去消费 `buildHomeSections(currentRole)`，移除扁平四宫格与 `canRead/canWrite/canLock/canUnlock` 直传模式。首页需按发现文档中的顺序渲染“主任务 / 高风险操作 / 管理工具”分组，显示每个入口的标题、简短说明和风险语义；无权限入口不渲染占位按钮。保留当前用户/角色摘要、角色切换和操作提示区，但让审计员进入首页时只看到日志/设置等管理工具，而不是一堆禁用主入口。</action>
  <verify>
    <automated>./gradlew assembleDebug</automated>
  </verify>
  <done>首页已不再是单一快捷操作网格；用户能从视觉层级上区分日常操作、高风险操作和管理工具，且无权限入口已被隐藏。</done>
</task>

<task type="auto">
  <name>Task 3: 底部导航改为按角色裁剪并与首页入口对齐</name>
  <files>app/src/main/java/com/opencode/nfccardmanager/navigation/AppNavGraph.kt, app/src/main/java/com/opencode/nfccardmanager/feature/home/HomeScreen.kt</files>
  <action>在 `AppNavGraph.kt` 中改用 `buildBottomNavDestinations(currentRole)` 生成底部导航项，并把 `HomeScreen` 的点击回调扩展到模板、日志、设置等管理工具入口，使首页与底部导航共享同一入口定义来源。继续沿用 Phase 1 的路由守卫作为兜底，但本任务重点是“入口前移裁剪”——例如模板页对非管理员不再出现在底部导航中，而不是点击后才看到拒绝访问。</action>
  <verify>
    <automated>./gradlew app:testDebugUnitTest --tests "*HomeShellContractTest" && ./gradlew assembleDebug</automated>
  </verify>
  <done>底部导航与首页入口已由同一角色契约驱动；用户不会在导航层看到明显不属于自己角色的一级入口。</done>
</task>

</tasks>

<verification>
- `assembleDebug` 通过，确保首页与导航重构没有破坏 Compose 编译与路由 wiring。
- `HomeShellContractTest` 仍通过，确保 UI 重构未绕开 Plan 01 的角色可见性约束。
</verification>

<success_criteria>
- SHELL-01：首页可直接区分主任务、高风险操作、管理工具。
- SHELL-02：首页与底部导航不再把当前角色无权入口展示成一级可操作入口。
</success_criteria>

<output>
After completion, create `.planning/phases/02-首页与导航重构/02-02-SUMMARY.md`
</output>
