---
phase: 06-验证闭环与回归基线
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/testing/phase-06-verification-baseline.md
  - docs/testing/phase-06-real-device-matrix.md
  - app/src/main/java/com/opencode/nfccardmanager/ui/test/AppTestTags.kt
  - app/src/androidTest/java/com/opencode/nfccardmanager/testutil/Phase6TestFixtures.kt
autonomous: true
requirements:
  - V2-08
must_haves:
  truths:
    - 团队可以用同一套验证术语描述权限、真实性、结果来源和审计语义，而不是继续依赖口头补充
    - 读卡、写卡、格式化、锁卡、解锁都具备可重复执行的真机验证矩阵，且每条用例都写清楚期望 UI 与期望审计结果
    - 后续 androidTest 可以复用稳定的测试标签和本地演示账号夹具，而不需要再次探索页面 copy 或账号规则
  artifacts:
    - path: docs/testing/phase-06-verification-baseline.md
      provides: Phase 6 自动化与人工验证共用的验证口径基线
      contains: verification objects, observation signals, pass criteria
    - path: docs/testing/phase-06-real-device-matrix.md
      provides: 五条主流程的真机 NFC 回归矩阵
      contains: preconditions, steps, expected ui, expected audit, recovery
    - path: app/src/main/java/com/opencode/nfccardmanager/ui/test/AppTestTags.kt
      provides: Phase 6 共享 UI 观测契约
      contains: login, home, risk, result, audit test tags
    - path: app/src/androidTest/java/com/opencode/nfccardmanager/testutil/Phase6TestFixtures.kt
      provides: 本地演示账号与角色映射测试夹具
      contains: phase6 accounts, roles, launch helpers
  key_links:
    - from: docs/testing/phase-06-verification-baseline.md
      to: app/src/main/java/com/opencode/nfccardmanager/ui/test/AppTestTags.kt
      via: 验证对象到 UI 观测节点命名对齐
      pattern: AppTestTags|验证对象|观测信号
    - from: docs/testing/phase-06-real-device-matrix.md
      to: app/src/main/java/com/opencode/nfccardmanager/core/database/AuditLogSemantics.kt
      via: 真机期望审计字段口径
      pattern: 角色|阶段|真实性|影响范围
---

<objective>
先把 Phase 6 的验证对象、观测信号和真机检查口径写成可执行基线，并为后续 androidTest 准备稳定的 UI 观测契约与账号夹具。

Purpose: `06-DISCOVERY.md` 已明确“先定义验证对象 / 观测信号 / 通过口径，再写测试实现”。如果直接堆 instrumentation 测试，后续最容易再次出现 copy 漂移、标签名漂移和真机检查口径分叉。
Output: 一份自动化/人工共用的验证基线、一份覆盖五条主流程的真机回归矩阵，以及一个供 06-02/06-03 直接消费的测试标签与账号夹具契约。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-验证闭环与回归基线/06-DISCOVERY.md
@.planning/phases/02-首页与导航重构/02-01-SUMMARY.md
@.planning/phases/03-高频流程澄清/03-01-SUMMARY.md
@.planning/phases/04-高风险流程强化/04-01-SUMMARY.md
@.planning/phases/05-辅助页统一收尾/05-03-SUMMARY.md
@app/build.gradle.kts

<interfaces>
From `app/src/main/java/com/opencode/nfccardmanager/feature/home/HomeShellContract.kt`:
```kotlin
fun buildHomeSections(role: UserRole): List<HomeSection>
fun buildBottomNavDestinations(role: UserRole): List<BottomNavDestination>
```

From `app/src/main/java/com/opencode/nfccardmanager/core/nfc/model/HighFrequencyFlowGuidance.kt`:
```kotlin
data class FlowNextStepGuidance(
    val title: String,
    val conclusion: String,
    val reasonSummary: String,
    val recommendedAction: String,
    val ctaLabel: String,
)

data class WriteOutcomeGuidance(
    val executionTitle: String,
    val executionConclusion: String,
    val verificationTitle: String,
    val verificationConclusion: String,
    val nextStep: FlowNextStepGuidance,
)
```

From `app/src/main/java/com/opencode/nfccardmanager/core/nfc/model/HighRiskFlowGuidance.kt`:
```kotlin
enum class HighRiskResultSource {
    CONFIRMED_EXECUTED,
    FAILED,
    UNVERIFIED,
    DEMO_ONLY,
}
```

From `app/src/main/java/com/opencode/nfccardmanager/core/database/AuditLogSemantics.kt`:
```kotlin
enum class AuditFlowStage(val storageValue: String, val label: String)
enum class AuditAuthenticity(val storageValue: String, val label: String)
enum class AuditImpactScope(val storageValue: String, val label: String)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 收口 Phase 6 验证基线与真机回归矩阵</name>
  <files>docs/testing/phase-06-verification-baseline.md, docs/testing/phase-06-real-device-matrix.md</files>
  <action>按 `06-DISCOVERY.md` 的优先级先写两份执行文档，而不是先写测试。`phase-06-verification-baseline.md` 必须明确列出：登录/首页权限、高风险真实性、路由守卫、高频结果页、审计语义这五类验证对象；每类对象都写清楚“观测信号、通过口径、自动化覆盖方式、真机补充方式”。`phase-06-real-device-matrix.md` 必须覆盖读卡、写卡、格式化、锁卡、解锁五条主流程，每条至少写明设备/系统、卡型/锁定方式、前置条件、触发步骤、期望 UI 阶段/真实性/结果来源、期望审计字段、失败恢复动作。术语必须复用 Phase 2-5 已落地的“真实性 / 推荐下一步 / 结果来源 / 影响范围”词汇，不要重新发明状态名，也不要弱化解锁 demo-only 边界。</action>
  <verify>
    <automated>python3 -c "from pathlib import Path; baseline = Path('docs/testing/phase-06-verification-baseline.md').read_text(); matrix = Path('docs/testing/phase-06-real-device-matrix.md').read_text(); assert all(item in baseline for item in ['登录/首页权限', '高风险真实性', '高频结果页', '审计语义']); assert all(flow in matrix for flow in ['读卡', '写卡', '格式化', '锁卡', '解锁'])"</automated>
  </verify>
  <done>两份文档都已存在，且能作为 06-02/06-03 自动化与后续真机回归的唯一验证口径来源。</done>
</task>

<task type="auto">
  <name>Task 2: 定义共享 UI 观测契约与测试账号夹具</name>
  <files>app/src/main/java/com/opencode/nfccardmanager/ui/test/AppTestTags.kt, app/src/androidTest/java/com/opencode/nfccardmanager/testutil/Phase6TestFixtures.kt</files>
  <action>新增 `AppTestTags.kt`，集中声明 Phase 6 要观测的稳定节点：登录用户名/密码/登录按钮、首页三段式 section、权限拒绝页根节点、锁卡/解锁真实性与结果来源块、读卡结果推荐动作块、写卡执行/校验/下一步三段块、格式化状态块、审计列表条目与详情分区。同步新增 `Phase6TestFixtures.kt`，固化本地演示账号（operator / supervisor / admin / auditor）及其角色期望，供 androidTest 直接复用。此任务只定义契约和夹具，不在这里把 tag 接入各页面；不要增加测试专用账号，不要增加登录 bypass，也不要引入新测试框架。</action>
  <verify>
    <automated>./gradlew assembleDebug assembleDebugAndroidTest</automated>
  </verify>
  <done>共享测试标签与账号夹具已可编译，06-02/06-03 可直接基于这些契约写 instrumentation 测试而无需再次探索页面结构。</done>
</task>

</tasks>

<verification>
- `phase-06-verification-baseline.md` 与 `phase-06-real-device-matrix.md` 均存在，并覆盖五条主流程。
- `assembleDebug assembleDebugAndroidTest` 通过，说明测试契约和 androidTest 夹具已可被后续计划复用。
</verification>

<success_criteria>
- Phase 6 的自动化与真机检查不再依赖口头约定，而有单一验证口径。
- 后续测试实现能直接使用稳定 test tags 与账号夹具，降低 instrumentation 首次落地成本。
</success_criteria>

<output>
After completion, create `.planning/phases/06-验证闭环与回归基线/06-01-SUMMARY.md`
</output>
