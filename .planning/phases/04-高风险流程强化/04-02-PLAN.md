---
phase: 04-高风险流程强化
plan: "02"
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - app/src/test/java/com/opencode/nfccardmanager/feature/lock/LockViewModelPhase4Test.kt
  - app/src/main/java/com/opencode/nfccardmanager/feature/lock/LockContract.kt
  - app/src/main/java/com/opencode/nfccardmanager/feature/lock/LockViewModel.kt
  - app/src/main/java/com/opencode/nfccardmanager/feature/lock/LockRiskScreen.kt
autonomous: true
requirements:
  - RISK-01
  - RISK-02
  - RISK-05
  - RISK-06
  - RISK-07
  - SUPP-03
must_haves:
  truths:
    - 用户在开始锁卡前，先看到风险摘要、确认条件、当前支持的锁卡方式与不可逆边界
    - 锁卡处理中用户不能误触返回或重复触发同一次高风险操作
    - 锁卡结果会清楚说明这是已确认执行、未验证还是失败，并给出与失败条件匹配的恢复建议，同时按角色遮罩敏感信息
  artifacts:
    - path: app/src/main/java/com/opencode/nfccardmanager/feature/lock/LockContract.kt
      provides: 锁卡页风险前置条件、结果来源、敏感信息展示状态
      contains: supportSummary/resultGuidance/maskedSensitiveFields
    - path: app/src/main/java/com/opencode/nfccardmanager/feature/lock/LockViewModel.kt
      provides: 锁卡结果到共享指导模型的映射
      contains: 对 HighRiskFlowGuidance 与 SensitiveDisplayPolicy 的消费
    - path: app/src/main/java/com/opencode/nfccardmanager/feature/lock/LockRiskScreen.kt
      provides: 锁卡风险摘要、处理中保护、结果来源与恢复建议 UI
      contains: 风险前置条件卡片、处理中离开保护、结果来源区
    - path: app/src/test/java/com/opencode/nfccardmanager/feature/lock/LockViewModelPhase4Test.kt
      provides: 锁卡 ViewModel 高风险指导回归测试
      contains: READY/LOCKING/SUCCESS/ERROR 与支持方式断言
  key_links:
    - from: app/src/main/java/com/opencode/nfccardmanager/feature/lock/LockViewModel.kt
      to: app/src/main/java/com/opencode/nfccardmanager/core/nfc/model/HighRiskFlowGuidance.kt
      via: buildLockSupportSummary/buildLockResultGuidance
      pattern: buildLock(SupportSummary|ResultGuidance)
    - from: app/src/main/java/com/opencode/nfccardmanager/feature/lock/LockRiskScreen.kt
      to: app/src/main/java/com/opencode/nfccardmanager/core/security/SensitiveDisplayPolicy.kt
      via: role-based masked result fields
      pattern: maskRiskSensitiveValue|masked
    - from: app/src/main/java/com/opencode/nfccardmanager/feature/lock/LockRiskScreen.kt
      to: app/src/main/java/com/opencode/nfccardmanager/core/nfc/NfcSessionManager.kt
      via: processing guard and session release
      pattern: requestReaderMode|releaseReaderMode|BackHandler
---

<objective>
把锁卡页升级为“风险前置条件 + 受控处理中保护 + 结果来源与恢复建议”的完整高风险流程页面，直接覆盖 `RISK-01`、`RISK-02`、`RISK-05`、`RISK-06`、`RISK-07` 与 `SUPP-03`。

Purpose: 锁卡具备不可逆风险，必须在执行前先说清楚支持方式与确认链路，在执行中防误触，在结果页说清楚是否真的执行成功以及失败后最安全的下一步。
Output: 锁卡 ViewModel 状态扩展、风险页面 UI 重构，以及对应的 ViewModel 单元测试。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-高频流程澄清/03-03-SUMMARY.md
@.planning/phases/04-高风险流程强化/04-01-PLAN.md
@app/src/main/java/com/opencode/nfccardmanager/feature/lock/LockContract.kt
@app/src/main/java/com/opencode/nfccardmanager/feature/lock/LockViewModel.kt
@app/src/main/java/com/opencode/nfccardmanager/feature/lock/LockRiskScreen.kt

<interfaces>
From `app/src/main/java/com/opencode/nfccardmanager/feature/lock/LockContract.kt`:
```kotlin
enum class LockStage {
    IDLE,
    READY,
    LOCKING,
    SUCCESS,
    ERROR,
}

data class LockUiState(
    val riskAcknowledged: Boolean = false,
    val confirmText: String = "",
    val stage: LockStage = LockStage.IDLE,
    val recommendedMode: LockMode = LockMode.NONE,
    val modeHint: String = "系统会优先尝试密码保护锁定；若卡片不支持，则降级为永久只读锁定。",
    val message: String = "确认风险后点击开始锁卡，系统才会进入识别与锁定流程。",
    val result: LockCardResult? = null,
)
```

Planned from `04-01` shared contract:
```kotlin
data class HighRiskSupportSummary(
    val title: String,
    val supportedLabel: String,
    val unsupportedLabel: String,
    val authenticityLabel: String,
)

data class HighRiskResultGuidance(
    val source: HighRiskResultSource,
    val conclusion: String,
    val recoveryAction: String,
    val ctaLabel: String,
)

fun buildLockSupportSummary(capability: CardCapability?): HighRiskSupportSummary
fun buildLockResultGuidance(result: LockCardResult?): HighRiskResultGuidance?
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 先锁定锁卡页高风险状态与结果指导测试</name>
  <files>app/src/test/java/com/opencode/nfccardmanager/feature/lock/LockViewModelPhase4Test.kt, app/src/main/java/com/opencode/nfccardmanager/feature/lock/LockContract.kt, app/src/main/java/com/opencode/nfccardmanager/feature/lock/LockViewModel.kt</files>
  <behavior>
    - Test 1: 风险确认未完成时，UI 明确显示前置条件不足，且不得进入可执行态。
    - Test 2: 识别到 `PASSWORD_PROTECTED` 与 `READ_ONLY_PERMANENT` 两类卡片时，UI 输出不同的支持方式摘要与风险提示。
    - Test 3: `onLockResult(success=true, verified=true)` 后，UI 标记“已确认执行”；`verified=false` 时标记“结果未验证”；失败时给出与原因匹配的恢复建议。
  </behavior>
  <action>新增 `LockViewModelPhase4Test.kt`，先锁定锁卡页对前置条件、支持方式、结果来源与恢复建议的状态输出。测试只围绕 `LockUiState` 断言，不引入 Compose；这样 Screen 只消费 ViewModel 已整理好的高风险语义。</action>
  <verify>
    <automated>./gradlew app:testDebugUnitTest --tests "*LockViewModelPhase4Test"</automated>
  </verify>
  <done>测试已覆盖锁卡前置条件、能力识别、已确认执行/未验证/失败三类关键状态，且在实现前失败或待实现。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 在锁卡状态层接入共享高风险指导与角色遮罩</name>
  <files>app/src/test/java/com/opencode/nfccardmanager/feature/lock/LockViewModelPhase4Test.kt, app/src/main/java/com/opencode/nfccardmanager/feature/lock/LockContract.kt, app/src/main/java/com/opencode/nfccardmanager/feature/lock/LockViewModel.kt</files>
  <behavior>
    - Test 1: `LockUiState` 能承载风险前置条件摘要、支持方式摘要、结果来源、恢复建议与遮罩后的敏感字段。
    - Test 2: `LockViewModel` 直接消费 `buildLockSupportSummary` / `buildLockResultGuidance`，而不是在 ViewModel 中散写状态判断。
  </behavior>
  <action>扩展 `LockContract.kt`，新增风险摘要、支持矩阵、结果来源、恢复建议和遮罩后展示字段。更新 `LockViewModel.kt`：识别卡片后直接消费 Plan 01 的共享指导契约生成支持方式说明；收到结果后输出 confirmed-executed / unverified / failed 等结果来源，并结合 `SensitiveDisplayPolicy` 生成适合当前角色展示的 UID/细节摘要。不要改写 `PasswordProtectedLocker` / `NdefLocker` 的底层 truth source，也不要让 Screen 自己拼接失败恢复建议。</action>
  <verify>
    <automated>./gradlew app:testDebugUnitTest --tests "*LockViewModelPhase4Test"</automated>
  </verify>
  <done>锁卡 ViewModel 已能稳定输出前置条件、支持方式、结果来源、恢复建议与角色分级遮罩信息，测试通过。</done>
</task>

<task type="auto">
  <name>Task 3: 重构锁卡页为受控高风险流程页面</name>
  <files>app/src/main/java/com/opencode/nfccardmanager/feature/lock/LockRiskScreen.kt</files>
  <action>重构 `LockRiskScreen.kt` 的信息结构：在操作按钮前明确展示“风险摘要 / 必需确认条件 / 当前支持方式与不支持方式 / 真实性与结果来源说明”；处理中新增保护层，至少包含禁用顶部返回、拦截系统返回（`BackHandler`）、禁用再次触发按钮与模拟按钮，并明确提示“处理中请勿离开或移开卡片”，满足 `RISK-05`。结果区改为“结果来源 + 发生了什么 + 为什么 + 当前最安全下一步”的结构：`verified=true` 显示已确认执行，`verified=false` 显示结果未验证，失败时按不支持/识别失败/校验失败输出不同恢复建议。对 UID 与锁卡细节使用 ViewModel 提供的遮罩字段；不要继续对所有角色直接渲染完整 UID，也不要只靠 `result.success` 决定全部文案。</action>
  <verify>
    <automated>./gradlew assembleDebug</automated>
  </verify>
  <done>锁卡页已先展示风险与支持边界，再受控进入扫描/处理中，结果区可区分已确认执行、未验证与失败，并按角色遮罩敏感信息。</done>
</task>

</tasks>

<verification>
- `LockViewModelPhase4Test` 通过，覆盖锁卡前置条件、支持方式、结果来源和恢复建议。
- `assembleDebug` 通过，说明锁卡页的新状态字段、处理中保护与结果区 wiring 完整可编译。
</verification>

<success_criteria>
- 用户在锁卡前能看懂风险摘要、确认链路和当前支持方式。
- 用户在锁卡处理中无法误触离开或重复触发同一操作。
- 用户在锁卡结果页可区分已确认执行、结果未验证与失败，并看到匹配的恢复建议与角色分级遮罩信息。
</success_criteria>

<output>
After completion, create `.planning/phases/04-高风险流程强化/04-02-SUMMARY.md`
</output>
