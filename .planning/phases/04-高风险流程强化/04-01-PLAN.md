---
phase: 04-高风险流程强化
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/java/com/opencode/nfccardmanager/core/nfc/model/HighRiskFlowGuidance.kt
  - app/src/main/java/com/opencode/nfccardmanager/core/security/SensitiveDisplayPolicy.kt
  - app/src/test/java/com/opencode/nfccardmanager/core/nfc/HighRiskFlowGuidanceTest.kt
autonomous: true
requirements:
  - RISK-02
  - RISK-03
  - RISK-06
  - RISK-07
  - SUPP-03
must_haves:
  truths:
    - 用户在进入锁卡或解锁结果区前，能从同一套规则看懂哪些方式已支持、哪些不支持、哪些只是演示
    - 用户看到高风险结果时，能区分已确认执行、执行失败、未验证与仅演示来源，而不是只看 success 布尔值
    - 不同角色查看高风险结果时，敏感卡片标识会按统一规则遮罩，而不是页面各自随意暴露
  artifacts:
    - path: app/src/main/java/com/opencode/nfccardmanager/core/nfc/model/HighRiskFlowGuidance.kt
      provides: 高风险流程共享指导与结果来源契约
      exports:
        - HighRiskSupportSummary
        - HighRiskResultGuidance
        - HighRiskResultSource
        - buildLockSupportSummary
        - buildUnlockSupportSummary
        - buildLockResultGuidance
        - buildUnlockResultGuidance
    - path: app/src/main/java/com/opencode/nfccardmanager/core/security/SensitiveDisplayPolicy.kt
      provides: 角色分级敏感信息展示策略
      exports:
        - SensitiveValueVisibility
        - maskRiskSensitiveValue
    - path: app/src/test/java/com/opencode/nfccardmanager/core/nfc/HighRiskFlowGuidanceTest.kt
      provides: 高风险指导契约回归测试
      contains: 支持矩阵、结果来源、恢复建议、角色遮罩断言
  key_links:
    - from: app/src/main/java/com/opencode/nfccardmanager/core/nfc/model/HighRiskFlowGuidance.kt
      to: app/src/main/java/com/opencode/nfccardmanager/core/nfc/model/CardModels.kt
      via: CardCapability.lockMode/canUnlock/requiresAuthForWrite
      pattern: lockMode|canUnlock|requiresAuthForWrite
    - from: app/src/main/java/com/opencode/nfccardmanager/core/nfc/model/HighRiskFlowGuidance.kt
      to: app/src/main/java/com/opencode/nfccardmanager/core/nfc/model/LockModels.kt
      via: LockCardResult.success/verified/verificationMessage
      pattern: success|verified|verificationMessage
    - from: app/src/main/java/com/opencode/nfccardmanager/core/nfc/model/HighRiskFlowGuidance.kt
      to: app/src/main/java/com/opencode/nfccardmanager/core/nfc/model/UnlockModels.kt
      via: UnlockCardResult.message/verificationMessage
      pattern: message|verificationMessage
---

<objective>
先把 Phase 4 需要的“高风险支持边界 + 结果来源 + 恢复建议 + 敏感信息展示规则”收口到纯 Kotlin 共享契约，给锁卡页和解锁页提供同一套 truth source。

Purpose: 避免锁卡与解锁页面分别散写支持矩阵、demo-only 提示、恢复建议和遮罩逻辑，确保 `RISK-02`、`RISK-03`、`RISK-06`、`RISK-07`、`SUPP-03` 在两个高风险页面里表达一致。
Output: 一个共享高风险指导契约、一个角色分级遮罩策略文件，以及覆盖关键语义的单元测试。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-高频流程澄清/03-03-SUMMARY.md
@app/src/main/java/com/opencode/nfccardmanager/core/nfc/model/CardModels.kt
@app/src/main/java/com/opencode/nfccardmanager/core/nfc/model/LockModels.kt
@app/src/main/java/com/opencode/nfccardmanager/core/nfc/model/UnlockModels.kt
@app/src/main/java/com/opencode/nfccardmanager/core/nfc/model/NfcFlowContract.kt
@app/src/main/java/com/opencode/nfccardmanager/core/security/SecurityManager.kt

<interfaces>
From `app/src/main/java/com/opencode/nfccardmanager/core/nfc/model/CardModels.kt`:
```kotlin
enum class LockMode {
    NONE,
    READ_ONLY_PERMANENT,
    PASSWORD_PROTECTED,
}

data class CardCapability(
    val canRead: Boolean,
    val canWrite: Boolean,
    val canLock: Boolean,
    val lockMode: LockMode,
    val canUnlock: Boolean,
    val requiresAuthForWrite: Boolean,
)
```

From `app/src/main/java/com/opencode/nfccardmanager/core/nfc/model/LockModels.kt`:
```kotlin
data class LockCardResult(
    val success: Boolean,
    val lockMode: LockMode = LockMode.NONE,
    val irreversible: Boolean = true,
    val verified: Boolean = false,
    val verificationMessage: String = "未校验",
)
```

From `app/src/main/java/com/opencode/nfccardmanager/core/nfc/model/UnlockModels.kt`:
```kotlin
data class UnlockCardResult(
    val success: Boolean,
    val message: String,
    val verificationMessage: String,
)
```

From `app/src/main/java/com/opencode/nfccardmanager/core/security/SecurityManager.kt`:
```kotlin
enum class UserRole {
    OPERATOR,
    SUPERVISOR,
    ADMIN,
    AUDITOR,
}

fun roleLabel(role: UserRole): String
fun maskUid(uid: String): String
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 先锁定高风险指导契约与敏感信息遮罩测试</name>
  <files>app/src/main/java/com/opencode/nfccardmanager/core/nfc/model/HighRiskFlowGuidance.kt, app/src/main/java/com/opencode/nfccardmanager/core/security/SensitiveDisplayPolicy.kt, app/src/test/java/com/opencode/nfccardmanager/core/nfc/HighRiskFlowGuidanceTest.kt</files>
  <behavior>
    - Test 1: `buildLockSupportSummary` 能区分密码保护、永久只读、不支持三类锁卡方式，并标明是否真实支持。
    - Test 2: `buildUnlockSupportSummary` 对 `canUnlock=false` 返回“不可解锁”，对 `canUnlock=true` 仍标记为“仅演示/流程骨架”。
    - Test 3: `buildLockResultGuidance` / `buildUnlockResultGuidance` 能区分 confirmed-executed、failed、unverified、demo-only 四类结果来源，并输出匹配的恢复建议。
    - Test 4: `maskRiskSensitiveValue` 对不同 `UserRole` 返回不同粒度的 UID/凭据展示结果，审计员与操作员不得看到完整敏感值。
  </behavior>
  <action>先创建 `HighRiskFlowGuidance.kt` 与 `SensitiveDisplayPolicy.kt` 的最小导出骨架，再编写 `HighRiskFlowGuidanceTest.kt`。测试必须显式断言“支持/不支持/仅演示”“结果来源”“恢复建议”“角色遮罩”四组语义，而不是只断言布尔值，避免后续锁卡页与解锁页继续各自拼接判断文案。</action>
  <verify>
    <automated>./gradlew app:testDebugUnitTest --tests "*HighRiskFlowGuidanceTest"</automated>
  </verify>
  <done>测试文件已描述 Phase 4 共享指导层必须满足的高风险语义，并在实现前失败或待实现。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现高风险共享指导契约与角色分级遮罩策略</name>
  <files>app/src/main/java/com/opencode/nfccardmanager/core/nfc/model/HighRiskFlowGuidance.kt, app/src/main/java/com/opencode/nfccardmanager/core/security/SensitiveDisplayPolicy.kt, app/src/test/java/com/opencode/nfccardmanager/core/nfc/HighRiskFlowGuidanceTest.kt</files>
  <behavior>
    - Test 1: 锁卡支持摘要直接消费 `CardCapability.lockMode/canLock`，不在页面层重写支持矩阵。
    - Test 2: 解锁支持摘要延续 Phase 1 决策：即使 `canUnlock=true`，当前结果来源仍为 `demo-only`，不可误写成真实已支持。
    - Test 3: 失败恢复建议根据“不支持、凭据错误、未校验、执行失败”输出不同文案。
    - Test 4: 角色遮罩策略可被锁卡页和解锁页直接复用，不需要页面再手写 `if (role == ...)` 规则。
  </behavior>
  <action>在 `HighRiskFlowGuidance.kt` 中实现纯 Kotlin 数据模型与构建函数，统一输出支持方式摘要、风险前置条件、结果来源标签、失败恢复建议和 CTA 文案；必须直接消费 `CardCapability`、`LockCardResult`、`UnlockCardResult` 这些既有 truth source，不要改写底层执行器结果。同步新增 `SensitiveDisplayPolicy.kt`，收口高风险页面对 UID、凭据摘要等敏感值的角色分级遮罩逻辑：管理员可见完整值，主管显示部分遮罩，操作员/审计员仅显示强遮罩或摘要，避免对所有角色无差别暴露。</action>
  <verify>
    <automated>./gradlew app:testDebugUnitTest --tests "*HighRiskFlowGuidanceTest"</automated>
  </verify>
  <done>共享契约已能稳定输出高风险支持边界、结果来源和恢复建议，且角色分级遮罩策略测试全部通过。</done>
</task>

</tasks>

<verification>
- `HighRiskFlowGuidanceTest` 通过，覆盖锁卡/解锁支持矩阵、结果来源、恢复建议与角色遮罩。
- 共享契约保持纯 Kotlin，无 Compose 依赖，可直接被 Lock/Unlock ViewModel 与 Screen 复用。
</verification>

<success_criteria>
- Phase 4 后续页面改造有统一的高风险指导 truth source，而不是锁卡页、解锁页各写一套判断。
- 解锁继续明确标记为 demo-only，且角色分级遮罩规则可被高风险页面一致复用。
</success_criteria>

<output>
After completion, create `.planning/phases/04-高风险流程强化/04-01-SUMMARY.md`
</output>
