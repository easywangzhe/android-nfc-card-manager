---
phase: 04-高风险流程强化
plan: "03"
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - app/src/test/java/com/opencode/nfccardmanager/feature/unlock/UnlockViewModelPhase4Test.kt
  - app/src/main/java/com/opencode/nfccardmanager/feature/unlock/UnlockContract.kt
  - app/src/main/java/com/opencode/nfccardmanager/feature/unlock/UnlockViewModel.kt
  - app/src/main/java/com/opencode/nfccardmanager/feature/unlock/UnlockVerifyScreen.kt
autonomous: true
requirements:
  - RISK-01
  - RISK-03
  - RISK-05
  - RISK-06
  - RISK-07
  - SUPP-03
must_haves:
  truths:
    - 用户在开始解锁前，先看到当前卡片场景是否可解锁、是真实支持还是仅演示，以及需要满足哪些前置条件
    - 解锁处理中用户不能误触返回或重复触发同一次操作
    - 解锁结果会明确标示 demo-only / failed / unsupported 来源，并给出针对实际失败条件的恢复建议，同时按角色遮罩敏感信息
  artifacts:
    - path: app/src/main/java/com/opencode/nfccardmanager/feature/unlock/UnlockContract.kt
      provides: 解锁页前置条件、支持边界、结果来源与遮罩状态
      contains: supportSummary/resultGuidance/maskedSensitiveFields
    - path: app/src/main/java/com/opencode/nfccardmanager/feature/unlock/UnlockViewModel.kt
      provides: 解锁结果到共享高风险指导模型的映射
      contains: 对 HighRiskFlowGuidance 与 SensitiveDisplayPolicy 的消费
    - path: app/src/main/java/com/opencode/nfccardmanager/feature/unlock/UnlockVerifyScreen.kt
      provides: 解锁能力边界、处理中保护、demo-only 结果表达 UI
      contains: 前置条件卡片、处理中离开保护、结果来源区
    - path: app/src/test/java/com/opencode/nfccardmanager/feature/unlock/UnlockViewModelPhase4Test.kt
      provides: 解锁 ViewModel 高风险指导回归测试
      contains: READY/SCANNING/SUCCESS/ERROR 与 demo-only 结果来源断言
  key_links:
    - from: app/src/main/java/com/opencode/nfccardmanager/feature/unlock/UnlockViewModel.kt
      to: app/src/main/java/com/opencode/nfccardmanager/core/nfc/model/HighRiskFlowGuidance.kt
      via: buildUnlockSupportSummary/buildUnlockResultGuidance
      pattern: buildUnlock(SupportSummary|ResultGuidance)
    - from: app/src/main/java/com/opencode/nfccardmanager/feature/unlock/UnlockVerifyScreen.kt
      to: app/src/main/java/com/opencode/nfccardmanager/core/nfc/UnlockExecutor.kt
      via: demo-only result disclosure and failure recovery UI
      pattern: execute|verificationMessage|BackHandler
    - from: app/src/main/java/com/opencode/nfccardmanager/feature/unlock/UnlockVerifyScreen.kt
      to: app/src/main/java/com/opencode/nfccardmanager/core/security/SensitiveDisplayPolicy.kt
      via: role-based masked result fields
      pattern: maskRiskSensitiveValue|masked
---

<objective>
把解锁页升级为“可解锁性边界先说明、demo-only 来源不混淆、处理中受控、失败恢复建议清晰”的高风险流程页面，覆盖 `RISK-01`、`RISK-03`、`RISK-05`、`RISK-06`、`RISK-07` 与 `SUPP-03`。

Purpose: 解锁当前仍有 demo 属性，最容易被用户误解成真实已支持；因此必须先说清楚当前场景是否可解锁、哪部分只是流程演示，并在处理中与结果页持续保持这一边界。
Output: 解锁 ViewModel 状态扩展、边界说明页面重构，以及对应的 ViewModel 单元测试。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-高频流程澄清/03-03-SUMMARY.md
@.planning/phases/04-高风险流程强化/04-01-PLAN.md
@app/src/main/java/com/opencode/nfccardmanager/feature/unlock/UnlockContract.kt
@app/src/main/java/com/opencode/nfccardmanager/feature/unlock/UnlockViewModel.kt
@app/src/main/java/com/opencode/nfccardmanager/feature/unlock/UnlockVerifyScreen.kt
@app/src/main/java/com/opencode/nfccardmanager/core/nfc/UnlockExecutor.kt

<interfaces>
From `app/src/main/java/com/opencode/nfccardmanager/feature/unlock/UnlockContract.kt`:
```kotlin
enum class UnlockStage {
    IDLE,
    READY,
    SCANNING,
    SUCCESS,
    ERROR,
}

data class UnlockUiState(
    val reason: String = "",
    val credential: String = "",
    val message: String = "请输入解锁理由和凭据，确认后点击开始解锁。",
    val stage: UnlockStage = UnlockStage.IDLE,
    val cardInfo: CardInfo? = null,
    val capability: CardCapability? = null,
    val result: UnlockCardResult? = null,
)
```

From `app/src/main/java/com/opencode/nfccardmanager/core/nfc/UnlockExecutor.kt`:
```kotlin
if (!capability.canUnlock || capability.lockMode != LockMode.PASSWORD_PROTECTED) {
    return UnlockCardResult(
        success = false,
        message = "当前卡片不支持通用解锁",
        verificationMessage = "若为 NDEF 永久只读锁定，则通常不可逆，无法解锁",
    )
}

return UnlockCardResult(
    success = true,
    message = "解锁流程骨架执行成功",
    verificationMessage = "当前版本仅完成密码保护型卡片的流程演示，未接入真实底层解除写保护命令",
)
```

Planned from `04-01` shared contract:
```kotlin
fun buildUnlockSupportSummary(capability: CardCapability?): HighRiskSupportSummary
fun buildUnlockResultGuidance(result: UnlockCardResult?): HighRiskResultGuidance?
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 先锁定解锁页可解锁性边界与 demo-only 结果测试</name>
  <files>app/src/test/java/com/opencode/nfccardmanager/feature/unlock/UnlockViewModelPhase4Test.kt, app/src/main/java/com/opencode/nfccardmanager/feature/unlock/UnlockContract.kt, app/src/main/java/com/opencode/nfccardmanager/feature/unlock/UnlockViewModel.kt</files>
  <behavior>
    - Test 1: 未填写理由/凭据时，UI 明确显示前置条件不足且不可启动解锁。
    - Test 2: 识别到永久只读或 `canUnlock=false` 卡片时，UI 输出“不可解锁/通常不可逆”的支持边界说明。
    - Test 3: 即使 `UnlockCardResult.success=true`，当前结果来源仍标记为 `demo-only`，并显示“流程演示成功但未接入真实底层命令”。
    - Test 4: 凭据错误与不支持解锁返回不同恢复建议，不能都写成笼统“请重试”。
  </behavior>
  <action>新增 `UnlockViewModelPhase4Test.kt`，先锁定解锁页对前置条件、可解锁性边界、demo-only 结果来源与失败恢复建议的状态输出。测试只断言 `UnlockUiState` 暴露的高风险指导字段，不引入 Compose 层依赖。</action>
  <verify>
    <automated>./gradlew app:testDebugUnitTest --tests "*UnlockViewModelPhase4Test"</automated>
  </verify>
  <done>测试已覆盖解锁前置条件、可解锁/不可解锁边界、demo-only 成功与失败恢复建议，且在实现前失败或待实现。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 在解锁状态层接入共享高风险指导与角色遮罩</name>
  <files>app/src/test/java/com/opencode/nfccardmanager/feature/unlock/UnlockViewModelPhase4Test.kt, app/src/main/java/com/opencode/nfccardmanager/feature/unlock/UnlockContract.kt, app/src/main/java/com/opencode/nfccardmanager/feature/unlock/UnlockViewModel.kt</files>
  <behavior>
    - Test 1: `UnlockUiState` 能承载前置条件摘要、可解锁性说明、结果来源、恢复建议与遮罩后的敏感字段。
    - Test 2: `UnlockViewModel` 直接消费 `buildUnlockSupportSummary` / `buildUnlockResultGuidance`，保持 demo-only 边界不漂移。
  </behavior>
  <action>扩展 `UnlockContract.kt`，新增前置条件、支持边界、结果来源、恢复建议和遮罩后展示字段。更新 `UnlockViewModel.kt`：识别卡片后直接消费 Plan 01 的共享指导契约生成“可解锁 / 不可解锁 / 仅演示”说明；收到结果后无论成功或失败都生成明确来源标签与恢复建议，并结合 `SensitiveDisplayPolicy` 输出当前角色可见的 UID/凭据摘要。必须延续 STATE.md 中“解锁仍带 demo 属性”的既定决策，不要把 `UnlockExecutor` 的流程骨架成功误标为真实解锁成功。</action>
  <verify>
    <automated>./gradlew app:testDebugUnitTest --tests "*UnlockViewModelPhase4Test"</automated>
  </verify>
  <done>解锁 ViewModel 已能稳定输出前置条件、可解锁性边界、demo-only 结果来源、恢复建议与角色分级遮罩信息，测试通过。</done>
</task>

<task type="auto">
  <name>Task 3: 重构解锁页为边界清晰的高风险流程页面</name>
  <files>app/src/main/java/com/opencode/nfccardmanager/feature/unlock/UnlockVerifyScreen.kt</files>
  <action>重构 `UnlockVerifyScreen.kt` 的信息结构：在开始按钮前明确展示“当前解锁范围 / 不支持场景 / demo-only 边界 / 必需前置条件”；识别卡片后把“可解锁性、真实性、恢复建议”放在同一区域连续表达，避免用户只看到按钮就误以为真实已支持。处理中新增保护层，至少包含禁用顶部返回、拦截系统返回（`BackHandler`）、禁用再次触发按钮与模拟按钮，并提示处理中不要离开或重复贴卡，满足 `RISK-05`。结果区改为“结果来源 + 发生了什么 + 为什么 + 当前最安全下一步”的结构：成功态仍明确标记 demo-only，失败态区分不支持解锁、凭据错误、识别失败三类恢复建议。对 UID 与凭据相关细节使用 ViewModel 提供的遮罩字段；不要继续对所有角色直接展示完整敏感值，也不要把 `success=true` 直接渲染成“真实解锁成功”。</action>
  <verify>
    <automated>./gradlew assembleDebug</automated>
  </verify>
  <done>解锁页已先表达可解锁性边界和 demo-only 前提，再受控进入扫描/处理中，结果区持续保持 demo-only 与失败恢复建议的清晰表达，并按角色遮罩敏感信息。</done>
</task>

</tasks>

<verification>
- `UnlockViewModelPhase4Test` 通过，覆盖前置条件、可解锁边界、demo-only 结果来源和失败恢复建议。
- `assembleDebug` 通过，说明解锁页的新状态字段、处理中保护与结果区 wiring 完整可编译。
</verification>

<success_criteria>
- 用户在解锁前能看懂当前场景是否可解锁、哪部分只是流程演示以及需要满足哪些前置条件。
- 用户在解锁处理中无法误触离开或重复触发同一操作。
- 用户在解锁结果页始终能区分 demo-only / failed / unsupported 来源，并看到匹配的恢复建议与角色分级遮罩信息。
</success_criteria>

<output>
After completion, create `.planning/phases/04-高风险流程强化/04-03-SUMMARY.md`
</output>
