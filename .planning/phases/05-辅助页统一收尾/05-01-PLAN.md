---
phase: 05-辅助页统一收尾
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/java/com/opencode/nfccardmanager/feature/support/SupportPageContract.kt
  - app/src/main/java/com/opencode/nfccardmanager/ui/component/AppUi.kt
  - app/src/test/java/com/opencode/nfccardmanager/feature/support/SupportPageContractTest.kt
autonomous: true
requirements:
  - SUPP-01
  - SUPP-04
must_haves:
  truths:
    - 用户进入模板、日志或设置页前，页面壳层已经有统一的摘要区、影响范围标签和分段结构来源
    - 执行阶段能复用同一套“安全性 / 可追责性 / 本地便利性”语义，而不是每页各写各的说明文案
  artifacts:
    - path: app/src/main/java/com/opencode/nfccardmanager/feature/support/SupportPageContract.kt
      provides: 辅助页统一摘要、区块、影响范围纯 Kotlin 契约
      contains: SupportPageSummary/SupportSection/SupportImpact
    - path: app/src/main/java/com/opencode/nfccardmanager/ui/component/AppUi.kt
      provides: 辅助页可复用的摘要卡片与影响标签 UI 入口
      contains: SupportPageSummaryCard/support impact badge helpers
    - path: app/src/test/java/com/opencode/nfccardmanager/feature/support/SupportPageContractTest.kt
      provides: 辅助页契约回归测试
      contains: impact mapping and default summary assertions
  key_links:
    - from: app/src/main/java/com/opencode/nfccardmanager/feature/support/SupportPageContract.kt
      to: app/src/main/java/com/opencode/nfccardmanager/ui/component/AppUi.kt
      via: shared summary and impact models
      pattern: SupportPage(Summary|Section|Impact)
---

<objective>
先补齐 Phase 5 的辅助页设计契约：把模板、日志、设置三页共用的摘要区、分段结构和“影响范围”语义先收口成单一来源，消除 UI gate。

Purpose: Phase 5 主要是辅助页统一收尾；如果没有先定义共享壳层与影响语义，后续页面实现会继续各写各的说明与布局，难以稳定满足 `SUPP-01` 与 `SUPP-04`。
Output: 一个可测试的辅助页共享契约，以及 `AppUi.kt` 中可复用的摘要/影响展示入口。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-首页与导航重构/02-01-SUMMARY.md
@app/src/main/java/com/opencode/nfccardmanager/ui/component/AppUi.kt
@app/src/main/java/com/opencode/nfccardmanager/feature/template/TemplateManagementScreen.kt
@app/src/main/java/com/opencode/nfccardmanager/feature/audit/AuditLogScreen.kt
@app/src/main/java/com/opencode/nfccardmanager/feature/settings/SettingsScreen.kt

<interfaces>
From `app/src/main/java/com/opencode/nfccardmanager/ui/component/AppUi.kt`:
```kotlin
@Composable
fun AppCard(
    modifier: Modifier = Modifier,
    content: @Composable () -> Unit,
)

@Composable fun StatusPill(text: String, tone: StatusTone)
@Composable fun SectionTitle(text: String)
fun Modifier.appPagePadding(innerPadding: PaddingValues): Modifier
```

Existing page pattern:
```kotlin
Scaffold(
    topBar = { AppTopBar(title = "...", onBack = onBack) },
) { paddingValues ->
    LazyColumn(
        modifier = Modifier.appPagePadding(paddingValues),
        verticalArrangement = Arrangement.spacedBy(16.dp),
    ) { ... }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 先锁定辅助页共享壳层与影响语义测试</name>
  <files>app/src/test/java/com/opencode/nfccardmanager/feature/support/SupportPageContractTest.kt, app/src/main/java/com/opencode/nfccardmanager/feature/support/SupportPageContract.kt</files>
  <behavior>
    - Test 1: 共享契约能把页面摘要分成 title / summary / impact / sections，供模板、日志、设置三页复用。
    - Test 2: `SupportImpact` 至少覆盖 `SAFETY`、`TRACEABILITY`、`LOCAL_CONVENIENCE` 三类，并能映射为稳定的中文标签与 tone。
    - Test 3: 未显式传入页面说明时，默认文案不得误导为“业务状态已变更”。
  </behavior>
  <action>新增 `SupportPageContractTest.kt`，先把 Phase 5 要复用的页面壳层契约锁定为纯 Kotlin 模型，不写 Compose 测试。重点锁定统一摘要区、影响范围标签和分段结构，延续 Phase 2 “单一契约来源”模式，避免模板/日志/设置页后续各自维护一套说明文案。</action>
  <verify>
    <automated>./gradlew app:testDebugUnitTest --tests "*SupportPageContractTest"</automated>
  </verify>
  <done>测试已明确辅助页共享摘要、影响范围语义和默认说明边界，且在实现前失败或待实现。</done>
</task>

<task type="auto">
  <name>Task 2: 实现辅助页共享设计契约与 AppUi 入口</name>
  <files>app/src/main/java/com/opencode/nfccardmanager/feature/support/SupportPageContract.kt, app/src/main/java/com/opencode/nfccardmanager/ui/component/AppUi.kt, app/src/test/java/com/opencode/nfccardmanager/feature/support/SupportPageContractTest.kt</files>
  <action>新增 `SupportPageContract.kt`，定义可供三个辅助页直接消费的 `SupportPageSummary`、`SupportSection`、`SupportImpact` 和 impact→tone/label 映射。同步在 `AppUi.kt` 增加辅助页摘要卡片与 impact pill 的通用 UI 入口，让后续页面只传契约，不再自行拼卡片结构。不要引入新依赖，不要做主题重写，也不要把页面私有文案硬编码进共享组件。</action>
  <verify>
    <automated>./gradlew app:testDebugUnitTest --tests "*SupportPageContractTest"</automated>
  </verify>
  <done>共享契约和通用 UI 入口已存在，后续计划可以直接基于同一套摘要/影响模型落地模板、日志、设置页面。</done>
</task>

</tasks>

<verification>
- `SupportPageContractTest` 通过，说明辅助页共享摘要和影响语义已被锁定。
- 共享契约只依赖现有 Kotlin + Compose 基建，无需额外 discovery 或外部服务。
</verification>

<success_criteria>
- Phase 5 已有可复用的辅助页壳层契约，不再依赖人工 UI gate 再解释结构。
- 三类影响范围（安全性 / 可追责性 / 本地便利性）成为单一来源，可被后续页面稳定消费。
</success_criteria>

<output>
After completion, create `.planning/phases/05-辅助页统一收尾/05-01-SUMMARY.md`
</output>
