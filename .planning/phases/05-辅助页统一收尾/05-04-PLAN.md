---
phase: 05-辅助页统一收尾
plan: "04"
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - app/src/test/java/com/opencode/nfccardmanager/feature/template/TemplateManagementViewModelPhase5Test.kt
  - app/src/main/java/com/opencode/nfccardmanager/feature/template/TemplateManagementViewModel.kt
  - app/src/main/java/com/opencode/nfccardmanager/feature/template/TemplateManagementScreen.kt
  - app/src/main/java/com/opencode/nfccardmanager/feature/settings/SettingsScreen.kt
autonomous: true
requirements:
  - SUPP-01
  - SUPP-04
must_haves:
  truths:
    - 模板页与设置页会使用和日志页同一套摘要区、影响标签和分段结构
    - 用户能看懂模板只是本地复用便利，不会误把模板编辑当成卡片已变更
    - 用户能区分“清理最近读卡缓存”只影响本地便利，而“清空本地审计日志”会影响可追责性
  artifacts:
    - path: app/src/main/java/com/opencode/nfccardmanager/feature/template/TemplateManagementViewModel.kt
      provides: 模板页共享摘要与本地便利说明状态
      contains: summary/message for local template impact
    - path: app/src/main/java/com/opencode/nfccardmanager/feature/template/TemplateManagementScreen.kt
      provides: 模板页统一辅助页结构
      contains: summary/editor/list sections
    - path: app/src/main/java/com/opencode/nfccardmanager/feature/settings/SettingsScreen.kt
      provides: 设置页影响范围解释与分离后的清理动作
      contains: local convenience vs traceability actions
    - path: app/src/test/java/com/opencode/nfccardmanager/feature/template/TemplateManagementViewModelPhase5Test.kt
      provides: 模板页状态语义回归测试
      contains: local-only guidance assertions
  key_links:
    - from: app/src/main/java/com/opencode/nfccardmanager/feature/template/TemplateManagementScreen.kt
      to: app/src/main/java/com/opencode/nfccardmanager/feature/support/SupportPageContract.kt
      via: shared summary and impact cards
      pattern: SupportPage|impact
    - from: app/src/main/java/com/opencode/nfccardmanager/feature/settings/SettingsScreen.kt
      to: app/src/main/java/com/opencode/nfccardmanager/core/database/AuditLogManager.kt
      via: traceability-sensitive clear action
      pattern: clearAll
    - from: app/src/main/java/com/opencode/nfccardmanager/feature/settings/SettingsScreen.kt
      to: app/src/main/java/com/opencode/nfccardmanager/feature/read/ReadResultStore.kt
      via: local convenience cache clear action
      pattern: ReadResultStore\.clear
---

<objective>
把模板页和设置页收口到统一的辅助页视觉/信息结构，同时明确说明哪些动作只影响本地便利，哪些动作会影响可追责性。

Purpose: Phase 5 不能只把日志页做好；模板和设置页仍是用户高频接触的辅助页，如果继续沿用当前“工具页拼装感”，`SUPP-01` 与 `SUPP-04` 依旧不成立。
Output: 模板页状态与布局重构、设置页影响说明与清理动作拆分，以及模板页状态测试。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-辅助页统一收尾/05-01-PLAN.md
@app/src/main/java/com/opencode/nfccardmanager/feature/template/TemplateManagementViewModel.kt
@app/src/main/java/com/opencode/nfccardmanager/feature/template/TemplateManagementScreen.kt
@app/src/main/java/com/opencode/nfccardmanager/feature/settings/SettingsScreen.kt

<interfaces>
From `app/src/main/java/com/opencode/nfccardmanager/feature/template/TemplateManagementViewModel.kt`:
```kotlin
data class TemplateManagementUiState(
    val templates: List<WriteTemplate> = emptyList(),
    val editingTemplateId: String? = null,
    val nameInput: String = "",
    val descriptionInput: String = "",
    val contentInput: String = "",
    val message: String = "可新增、编辑、删除本地模板。",
)
```

From `app/src/main/java/com/opencode/nfccardmanager/feature/settings/SettingsScreen.kt`:
```kotlin
var cacheMessage by remember { mutableStateOf("可清理本地审计日志与最近读卡缓存") }
AuditLogManager.clearAll()
ReadResultStore.clear()
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 先锁定模板页的本地便利说明状态测试</name>
  <files>app/src/test/java/com/opencode/nfccardmanager/feature/template/TemplateManagementViewModelPhase5Test.kt, app/src/main/java/com/opencode/nfccardmanager/feature/template/TemplateManagementViewModel.kt</files>
  <behavior>
    - Test 1: 默认态明确说明模板仅影响本地复用效率，不代表卡片内容已变更。
    - Test 2: 进入编辑模式时，状态文案仍需保留“本地模板”边界，而不是暗示线上或设备同步。
    - Test 3: 保存/删除后反馈文案与页面摘要不能丢失影响范围语义。
  </behavior>
  <action>新增 `TemplateManagementViewModelPhase5Test.kt`，先把模板页的本地便利性说明锁定在 ViewModel 状态层。测试不要依赖 Compose，只断言 `TemplateManagementUiState` 文案与摘要字段，避免页面重构时再次把模板误表述为真实业务状态。</action>
  <verify>
    <automated>./gradlew app:testDebugUnitTest --tests "*TemplateManagementViewModelPhase5Test"</automated>
  </verify>
  <done>模板页“仅影响本地复用便利”的语义已被测试锁定，且在实现前失败或待实现。</done>
</task>

<task type="auto">
  <name>Task 2: 用统一辅助页壳层重构模板页</name>
  <files>app/src/main/java/com/opencode/nfccardmanager/feature/template/TemplateManagementViewModel.kt, app/src/main/java/com/opencode/nfccardmanager/feature/template/TemplateManagementScreen.kt, app/src/test/java/com/opencode/nfccardmanager/feature/template/TemplateManagementViewModelPhase5Test.kt</files>
  <action>扩展 `TemplateManagementViewModel.kt`，补齐可供页面直接消费的摘要/影响语义，让模板页始终明确“模板只影响后续写卡复用效率，本身不会修改当前卡片”。重构 `TemplateManagementScreen.kt` 为统一辅助页结构：顶部摘要卡说明用途与影响范围；中段保留编辑区；下段模板列表突出名称、版本、说明与本地属性。沿用 05-01 共享契约，不要新增独立 UI 体系，也不要把模板列表渲染成缺少上下文的裸按钮堆。</action>
  <verify>
    <automated>./gradlew app:testDebugUnitTest --tests "*TemplateManagementViewModelPhase5Test" && ./gradlew assembleDebug</automated>
  </verify>
  <done>模板页已使用统一摘要与影响语义，用户可清楚理解模板只是本地复用工具，不会误判为卡片状态已改变。</done>
</task>

<task type="auto">
  <name>Task 3: 重构设置页的影响说明与清理动作边界</name>
  <files>app/src/main/java/com/opencode/nfccardmanager/feature/settings/SettingsScreen.kt</files>
  <action>重构 `SettingsScreen.kt` 的信息结构：顶部摘要明确区分账号/NFC 状态、追责记录和本地缓存。把当前“清理缓存”混合动作拆成至少两个独立入口：一个只清理最近读卡缓存（标记 `LOCAL_CONVENIENCE`），一个清空本地审计日志（标记 `TRACEABILITY`）；每个动作前都要有清晰说明，避免用户把普通清理操作误认为卡片业务状态变化。保留 logout，但不要把“退出登录”与本地清理混在同一个说明块里。</action>
  <verify>
    <automated>./gradlew assembleDebug</automated>
  </verify>
  <done>设置页已明确表达各动作影响的是本地便利还是可追责性，且清理动作边界不再混淆。</done>
</task>

</tasks>

<verification>
- `TemplateManagementViewModelPhase5Test` 通过，锁定模板页的本地便利语义。
- `assembleDebug` 通过，说明模板页与设置页的新结构和动作 wiring 完整可编译。
</verification>

<success_criteria>
- 模板、日志、设置三类辅助页已共享统一摘要与影响语义来源。
- 用户可以明确分辨模板/缓存/审计清理分别影响的是复用效率、追责记录还是登录便利，而不是卡片业务状态。
</success_criteria>

<output>
After completion, create `.planning/phases/05-辅助页统一收尾/05-04-SUMMARY.md`
</output>
