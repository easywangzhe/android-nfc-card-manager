---
phase: 05-辅助页统一收尾
plan: "02"
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - app/src/main/java/com/opencode/nfccardmanager/core/database/AuditLogSemantics.kt
  - app/src/main/java/com/opencode/nfccardmanager/core/database/AuditLogModels.kt
  - app/src/main/java/com/opencode/nfccardmanager/core/database/AuditLogManager.kt
  - app/src/main/java/com/opencode/nfccardmanager/core/database/AuditLogRepository.kt
  - app/src/main/java/com/opencode/nfccardmanager/core/database/AuditLogDbHelper.kt
  - app/src/test/java/com/opencode/nfccardmanager/core/database/AuditLogSemanticsTest.kt
autonomous: true
requirements:
  - SUPP-02
  - SUPP-04
must_haves:
  truths:
    - 审计记录的数据模型本身能够表达操作者角色、流程阶段、真实性状态和影响范围，而不是只剩一段 message
    - 历史日志在升级后仍可读取，并以明确 fallback 文案显示未知字段而非空白
  artifacts:
    - path: app/src/main/java/com/opencode/nfccardmanager/core/database/AuditLogSemantics.kt
      provides: 审计元数据枚举、标签与历史记录 fallback 规则
      contains: role/stage/authenticity/impact semantics
    - path: app/src/main/java/com/opencode/nfccardmanager/core/database/AuditLogModels.kt
      provides: 富化后的 AuditLogRecord 模型
      contains: operatorRole/flowStage/authenticity/impactScope
    - path: app/src/main/java/com/opencode/nfccardmanager/core/database/AuditLogDbHelper.kt
      provides: 审计元数据落库与历史库迁移
      contains: DB_VERSION 2 and onUpgrade
    - path: app/src/test/java/com/opencode/nfccardmanager/core/database/AuditLogSemanticsTest.kt
      provides: 审计元数据标签与 fallback 回归测试
      contains: legacy/default assertions
  key_links:
    - from: app/src/main/java/com/opencode/nfccardmanager/core/database/AuditLogManager.kt
      to: app/src/main/java/com/opencode/nfccardmanager/core/database/AuditLogModels.kt
      via: expanded save signature
      pattern: AuditLogRecord\(
    - from: app/src/main/java/com/opencode/nfccardmanager/core/database/AuditLogDbHelper.kt
      to: app/src/main/java/com/opencode/nfccardmanager/core/database/AuditLogSemantics.kt
      via: persisted metadata defaults and migration fallback
      pattern: operator_role|flow_stage|authenticity|impact_scope
---

<objective>
把审计模型从“只有操作、结果和 message”升级为“能表达角色、阶段、真实性和影响范围”的持久化基础，为日志页可读性和追责说明打底。

Purpose: `SUPP-02` 的核心不是改几段 UI 文案，而是让审计数据自身携带足够上下文；否则日志页再怎么排版，也无法回答“谁以什么角色在什么阶段、以什么真实性状态做了什么”。
Output: 富化后的审计元数据契约、数据库迁移与保存入口。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-高风险流程强化/04-01-SUMMARY.md
@app/src/main/java/com/opencode/nfccardmanager/core/database/AuditLogModels.kt
@app/src/main/java/com/opencode/nfccardmanager/core/database/AuditLogManager.kt
@app/src/main/java/com/opencode/nfccardmanager/core/database/AuditLogDbHelper.kt

<interfaces>
From `app/src/main/java/com/opencode/nfccardmanager/core/database/AuditLogModels.kt`:
```kotlin
data class AuditLogRecord(
    val id: Long = 0,
    val operationType: String,
    val operatorId: String,
    val cardUidMasked: String,
    val cardType: String,
    val result: String,
    val message: String,
    val createdAt: Long,
)
```

From `app/src/main/java/com/opencode/nfccardmanager/core/database/AuditLogManager.kt`:
```kotlin
fun save(
    operationType: String,
    cardUid: String,
    cardType: String,
    result: String,
    message: String,
    operatorId: String = "system",
)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 先锁定审计元数据语义与历史 fallback 测试</name>
  <files>app/src/test/java/com/opencode/nfccardmanager/core/database/AuditLogSemanticsTest.kt, app/src/main/java/com/opencode/nfccardmanager/core/database/AuditLogSemantics.kt</files>
  <behavior>
    - Test 1: 审计语义必须稳定覆盖 `operatorRole`、`flowStage`、`authenticity`、`impactScope` 四类元数据，并映射为中文展示标签。
    - Test 2: 历史记录缺失元数据时，fallback 文案必须显式表达“历史记录 / 未标记 / 待补充”，不能出现空字符串。
    - Test 3: `impactScope` 至少能区分 `TRACEABILITY` 与 `LOCAL_CONVENIENCE`，避免设置页清理操作被误读为业务状态变更。
  </behavior>
  <action>新增 `AuditLogSemanticsTest.kt`，先把审计元数据的枚举、中文标签和历史记录 fallback 规则锁定为纯 Kotlin 测试。测试不要依赖 SQLite 或 Android 上下文，只验证语义层，确保后续数据库升级与 UI 展示都消费同一来源。</action>
  <verify>
    <automated>./gradlew app:testDebugUnitTest --tests "*AuditLogSemanticsTest"</automated>
  </verify>
  <done>审计元数据语义与历史 fallback 已在纯 Kotlin 测试中被锁定，且在实现前失败或待实现。</done>
</task>

<task type="auto">
  <name>Task 2: 扩展审计模型、保存入口与数据库迁移</name>
  <files>app/src/main/java/com/opencode/nfccardmanager/core/database/AuditLogSemantics.kt, app/src/main/java/com/opencode/nfccardmanager/core/database/AuditLogModels.kt, app/src/main/java/com/opencode/nfccardmanager/core/database/AuditLogManager.kt, app/src/main/java/com/opencode/nfccardmanager/core/database/AuditLogRepository.kt, app/src/main/java/com/opencode/nfccardmanager/core/database/AuditLogDbHelper.kt, app/src/test/java/com/opencode/nfccardmanager/core/database/AuditLogSemanticsTest.kt</files>
  <action>实现 `AuditLogSemantics.kt`，定义审计角色、流程阶段、真实性状态和影响范围枚举/标签。扩展 `AuditLogRecord` 与 `AuditLogManager.save(...)`，让调用方可以传入这些元数据，同时保留对旧调用的兼容默认值。升级 `AuditLogDbHelper.kt` 到新版本，新增元数据列并在 `onUpgrade` 中为历史库回填稳定 fallback；不要清空旧表重建，也不要把旧日志 silently 丢弃。保持 offline-first，本计划只做本地 SQLite 迁移，不引入任何远程依赖。</action>
  <verify>
    <automated>./gradlew app:testDebugUnitTest --tests "*AuditLogSemanticsTest" && ./gradlew assembleDebug</automated>
  </verify>
  <done>审计记录已可持久化角色/阶段/真实性/影响范围，历史数据升级后仍可读取且有明确 fallback 文案。</done>
</task>

</tasks>

<verification>
- `AuditLogSemanticsTest` 通过，说明元数据语义与 fallback 不漂移。
- `assembleDebug` 通过，说明审计模型、管理器和 SQLite 升级 wiring 完整可编译。
</verification>

<success_criteria>
- 审计数据层已经具备回答“谁 / 角色 / 阶段 / 真实性 / 影响范围”的最小事实集合。
- 数据库升级不会抹掉历史日志，旧记录也能被后续日志页稳定展示。
</success_criteria>

<output>
After completion, create `.planning/phases/05-辅助页统一收尾/05-02-SUMMARY.md`
</output>
