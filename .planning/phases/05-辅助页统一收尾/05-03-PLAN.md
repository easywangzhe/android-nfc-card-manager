---
phase: 05-辅助页统一收尾
plan: "03"
type: execute
wave: 3
depends_on:
  - 05-01
  - 05-02
files_modified:
  - app/src/main/java/com/opencode/nfccardmanager/feature/audit/AuditLogPresentation.kt
  - app/src/main/java/com/opencode/nfccardmanager/feature/audit/AuditLogViewModel.kt
  - app/src/main/java/com/opencode/nfccardmanager/feature/audit/AuditLogScreen.kt
  - app/src/main/java/com/opencode/nfccardmanager/feature/audit/AuditLogDetailScreen.kt
  - app/src/main/java/com/opencode/nfccardmanager/feature/scan/ScanViewModel.kt
  - app/src/main/java/com/opencode/nfccardmanager/feature/write/WriteViewModel.kt
  - app/src/main/java/com/opencode/nfccardmanager/feature/format/FormatViewModel.kt
  - app/src/main/java/com/opencode/nfccardmanager/feature/lock/LockViewModel.kt
  - app/src/main/java/com/opencode/nfccardmanager/feature/unlock/UnlockViewModel.kt
  - app/src/test/java/com/opencode/nfccardmanager/feature/audit/AuditLogViewModelPhase5Test.kt
autonomous: true
requirements:
  - SUPP-01
  - SUPP-02
  - SUPP-04
must_haves:
  truths:
    - 用户查看任一审计记录时，能直接读出是谁、以什么角色、处于哪个阶段、以什么真实性状态执行了什么操作
    - 日志列表和详情页沿用同一套摘要与影响语义，不再只是“结果 + message”堆叠
    - 新产生的读卡、写卡、格式化、锁卡、解锁日志都会写入完整元数据，而不是留空等待 UI 猜测
  artifacts:
    - path: app/src/main/java/com/opencode/nfccardmanager/feature/audit/AuditLogPresentation.kt
      provides: 审计记录展示摘要与标签映射
      contains: list/detail presentation builders
    - path: app/src/main/java/com/opencode/nfccardmanager/feature/audit/AuditLogScreen.kt
      provides: 富化后的审计列表页
      contains: overview/impact/context badges
    - path: app/src/main/java/com/opencode/nfccardmanager/feature/audit/AuditLogDetailScreen.kt
      provides: 富化后的审计详情页
      contains: who-role-stage-authenticity sections
    - path: app/src/test/java/com/opencode/nfccardmanager/feature/audit/AuditLogViewModelPhase5Test.kt
      provides: 审计展示语义回归测试
      contains: metadata projection assertions
  key_links:
    - from: app/src/main/java/com/opencode/nfccardmanager/feature/scan/ScanViewModel.kt
      to: app/src/main/java/com/opencode/nfccardmanager/core/database/AuditLogManager.kt
      via: enriched save metadata for READ
      pattern: AuditLogManager\.save\(
    - from: app/src/main/java/com/opencode/nfccardmanager/feature/audit/AuditLogViewModel.kt
      to: app/src/main/java/com/opencode/nfccardmanager/feature/audit/AuditLogPresentation.kt
      via: viewmodel-built display cards
      pattern: buildAuditLog
    - from: app/src/main/java/com/opencode/nfccardmanager/feature/audit/AuditLogScreen.kt
      to: app/src/main/java/com/opencode/nfccardmanager/feature/support/SupportPageContract.kt
      via: shared summary and impact badges
      pattern: SupportPage|impact
---

<objective>
把 Phase 5 的审计页真正落地：一方面让各操作流写入完整审计元数据，另一方面让日志列表/详情页用统一信息层级读出“谁、角色、阶段、真实性、影响范围”。

Purpose: `SUPP-02` 只有在“数据写得对 + UI 读得清”同时成立时才算完成；只改 UI 会继续猜数据，只改保存入口又无法让用户读懂。
Output: 富化后的审计写入调用、审计展示映射层、审计列表页与详情页改造，以及对应 ViewModel 测试。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-高频流程澄清/03-03-SUMMARY.md
@.planning/phases/04-高风险流程强化/04-01-SUMMARY.md
@.planning/phases/05-辅助页统一收尾/05-01-PLAN.md
@.planning/phases/05-辅助页统一收尾/05-02-PLAN.md
@app/src/main/java/com/opencode/nfccardmanager/feature/audit/AuditLogViewModel.kt
@app/src/main/java/com/opencode/nfccardmanager/feature/audit/AuditLogScreen.kt
@app/src/main/java/com/opencode/nfccardmanager/feature/audit/AuditLogDetailScreen.kt

<interfaces>
From `05-02` planned audit model:
```kotlin
data class AuditLogRecord(
    val operatorRole: AuditOperatorRole,
    val flowStage: AuditFlowStage,
    val authenticity: AuditAuthenticity,
    val impactScope: AuditImpactScope,
    // existing fields omitted
)
```

From `app/src/main/java/com/opencode/nfccardmanager/feature/audit/AuditLogViewModel.kt`:
```kotlin
data class AuditLogUiState(
    val logs: List<AuditLogRecord> = emptyList(),
    val filteredLogs: List<AuditLogRecord> = emptyList(),
    val isLoading: Boolean = false,
    val operationFilter: String = "ALL",
    val resultFilter: AuditResultFilter = AuditResultFilter.ALL,
    val keyword: String = "",
)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 先锁定审计展示映射与 ViewModel 回归测试</name>
  <files>app/src/test/java/com/opencode/nfccardmanager/feature/audit/AuditLogViewModelPhase5Test.kt, app/src/main/java/com/opencode/nfccardmanager/feature/audit/AuditLogPresentation.kt, app/src/main/java/com/opencode/nfccardmanager/feature/audit/AuditLogViewModel.kt</files>
  <behavior>
    - Test 1: 列表态至少能从 `AuditLogRecord` 投影出操作人、角色、阶段、真实性和影响范围标签，历史 fallback 也要可读。
    - Test 2: 详情态必须把“谁执行 / 当前阶段 / 真实性边界 / 影响范围 / 发生了什么”拆成稳定展示字段，而不是直接拼接 message。
    - Test 3: 关键字与结果筛选回归不能被新的展示层破坏。
  </behavior>
  <action>新增 `AuditLogPresentation.kt` 与 `AuditLogViewModelPhase5Test.kt`，先把审计列表/详情的展示映射层锁定为纯 Kotlin。测试只断言 ViewModel / presentation 输出，不做 Compose instrumentation，避免 UI 重构后再次散写字段拼接逻辑。</action>
  <verify>
    <automated>./gradlew app:testDebugUnitTest --tests "*AuditLogViewModelPhase5Test"</automated>
  </verify>
  <done>审计展示映射和筛选逻辑已被测试锁定，且在实现前失败或待实现。</done>
</task>

<task type="auto">
  <name>Task 2: 为各操作流写入完整审计元数据</name>
  <files>app/src/main/java/com/opencode/nfccardmanager/feature/scan/ScanViewModel.kt, app/src/main/java/com/opencode/nfccardmanager/feature/write/WriteViewModel.kt, app/src/main/java/com/opencode/nfccardmanager/feature/format/FormatViewModel.kt, app/src/main/java/com/opencode/nfccardmanager/feature/lock/LockViewModel.kt, app/src/main/java/com/opencode/nfccardmanager/feature/unlock/UnlockViewModel.kt</files>
  <action>更新五个操作 ViewModel 的 `AuditLogManager.save(...)` 调用：统一传入当前操作者角色、最终流程阶段、真实性状态和影响范围。真实性必须复用 Phase 1/3/4 已建立的 capability/guidance 边界，尤其保持“解锁成功仍是 demo-only”的既定决策；不要从中文 message 文案反推真实性。影响范围对这些审计记录统一标记为 `TRACEABILITY`，让用户明确这些记录服务于追责，而不是直接改变卡片业务状态。</action>
  <verify>
    <automated>./gradlew assembleDebug</automated>
  </verify>
  <done>新生成的 READ/WRITE/FORMAT/LOCK/UNLOCK 审计记录都写入完整元数据，真实性边界与既有 Phase 决策保持一致。</done>
</task>

<task type="auto">
  <name>Task 3: 重构日志列表与详情页为可读的辅助页结构</name>
  <files>app/src/main/java/com/opencode/nfccardmanager/feature/audit/AuditLogPresentation.kt, app/src/main/java/com/opencode/nfccardmanager/feature/audit/AuditLogViewModel.kt, app/src/main/java/com/opencode/nfccardmanager/feature/audit/AuditLogScreen.kt, app/src/main/java/com/opencode/nfccardmanager/feature/audit/AuditLogDetailScreen.kt, app/src/test/java/com/opencode/nfccardmanager/feature/audit/AuditLogViewModelPhase5Test.kt</files>
  <action>让 `AuditLogViewModel.kt` 消费 `AuditLogPresentation.kt`，把列表卡片和详情页统一改为 Phase 5 的辅助页结构：顶部总览摘要明确说明“本页用于追责，不直接改变卡片状态”；列表卡片至少展示操作、操作者、角色、阶段、真实性和时间；详情页拆出“谁执行了什么 / 结果来源与真实性 / 影响范围 / 说明与后续参考”区块，并对历史记录显示 fallback 标签。统一复用 05-01 的 support page impact 语义，不要继续只显示 `result + message`，也不要保留 `CenterAlignedTopAppBar` 特例而绕开 `AppTopBar`。</action>
  <verify>
    <automated>./gradlew app:testDebugUnitTest --tests "*AuditLogViewModelPhase5Test" && ./gradlew assembleDebug</automated>
  </verify>
  <done>日志列表和详情页已能稳定读出谁/角色/阶段/真实性/影响范围，并与辅助页统一结构保持一致。</done>
</task>

</tasks>

<verification>
- `AuditLogViewModelPhase5Test` 通过，锁定审计展示映射与筛选回归。
- `assembleDebug` 通过，说明审计元数据写入与日志页面 UI wiring 完整。
</verification>

<success_criteria>
- 审计页不再像原始技术日志，而是能让用户直接读懂追责上下文。
- 新写入的操作日志携带完整元数据，日志页不需要再靠猜测补字段。
</success_criteria>

<output>
After completion, create `.planning/phases/05-辅助页统一收尾/05-03-SUMMARY.md`
</output>
